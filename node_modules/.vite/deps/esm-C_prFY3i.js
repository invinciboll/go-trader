//#region node_modules/@yume-chan/async/esm/promise-resolver.js
var PromiseResolver = class {
	#promise;
	get promise() {
		return this.#promise;
	}
	#resolve;
	#reject;
	#state = "running";
	get state() {
		return this.#state;
	}
	constructor() {
		this.#promise = new Promise((resolve, reject) => {
			this.#resolve = resolve;
			this.#reject = reject;
		});
	}
	resolve = (value) => {
		this.#resolve(value);
		this.#state = "resolved";
	};
	reject = (reason) => {
		this.#reject(reason);
		this.#state = "rejected";
	};
};
//#endregion
//#region node_modules/@yume-chan/async/esm/async-operation-manager.js
var AsyncOperationManager = class {
	nextId;
	pendingResolvers = /* @__PURE__ */ new Map();
	constructor(startId = 0) {
		this.nextId = startId;
	}
	add() {
		const id = this.nextId++;
		const resolver = new PromiseResolver();
		this.pendingResolvers.set(id, resolver);
		return [id, resolver.promise];
	}
	getResolver(id) {
		if (!this.pendingResolvers.has(id)) return null;
		const resolver = this.pendingResolvers.get(id);
		this.pendingResolvers.delete(id);
		return resolver;
	}
	resolve(id, result) {
		const resolver = this.getResolver(id);
		if (resolver !== null) {
			resolver.resolve(result);
			return true;
		}
		return false;
	}
	reject(id, reason) {
		const resolver = this.getResolver(id);
		if (resolver !== null) {
			resolver.reject(reason);
			return true;
		}
		return false;
	}
};
//#endregion
//#region node_modules/@yume-chan/async/esm/delay.js
function delay(time) {
	return new Promise((resolve) => {
		globalThis.setTimeout(() => resolve(), time);
	});
}
//#endregion
//#region node_modules/@yume-chan/async/esm/maybe-promise.js
function isPromiseLike(value) {
	return typeof value === "object" && value !== null && "then" in value;
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/bipedal.js
function advance(iterator, next) {
	while (true) {
		const { done, value } = iterator.next(next);
		if (done) return value;
		if (isPromiseLike(value)) return value.then((value) => advance(iterator, { resolved: value }), (error) => advance(iterator, { error }));
		next = value;
	}
}
/* @__NO_SIDE_EFFECTS__ */
function bipedal(fn, bindThis) {
	function result(...args) {
		return advance(fn.call(this, function* (value) {
			if (isPromiseLike(value)) {
				const result = yield value;
				if ("resolved" in result) return result.resolved;
				else throw result.error;
			}
			return value;
		}, ...args), void 0);
	}
	if (bindThis) return result.bind(bindThis);
	else return result;
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/field/serialize.js
function defaultFieldSerializer(serializer) {
	return (source, context) => {
		if ("buffer" in context) {
			const buffer = serializer(source, context);
			context.buffer.set(buffer, context.index);
			return buffer.length;
		} else return serializer(source, context);
	};
}
function byobFieldSerializer(size, serializer) {
	return (source, context) => {
		if ("buffer" in context) {
			context.index ??= 0;
			serializer(source, context);
			return size;
		} else {
			const buffer = new Uint8Array(size);
			serializer(source, {
				buffer,
				index: 0,
				littleEndian: context.littleEndian
			});
			return buffer;
		}
	};
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/field/factory.js
/* @__NO_SIDE_EFFECTS__ */
function _field(size, type, serialize, deserialize, options) {
	const field = {
		size,
		type,
		serialize: type === "default" ? defaultFieldSerializer(serialize) : byobFieldSerializer(size, serialize),
		deserialize: /* @__PURE__ */ bipedal(deserialize),
		omitInit: options?.omitInit
	};
	if (options?.init) field.init = options.init;
	return field;
}
var field = _field;
//#endregion
//#region node_modules/@yume-chan/struct/esm/buffer.js
var EmptyUint8Array = new Uint8Array(0);
function copyMaybeDifferentLength(dest, source, index, length) {
	if (source.length < length) {
		dest.set(source, index);
		dest.fill(0, index + source.length, index + length);
	} else if (source.length === length) dest.set(source, index);
	else dest.set(source.subarray(0, length), index);
}
/* @__NO_SIDE_EFFECTS__ */
function buffer(lengthOrField, converter) {
	if (typeof lengthOrField === "number") {
		let serialize;
		let deserialize;
		let init;
		if (lengthOrField === 0) {
			serialize = () => {};
			if (converter) deserialize = function* () {
				return converter.convert(EmptyUint8Array);
			};
			else deserialize = function* () {
				return EmptyUint8Array;
			};
		} else {
			serialize = (value, { buffer, index }) => copyMaybeDifferentLength(buffer, value, index, lengthOrField);
			if (converter) {
				deserialize = function* (then, reader) {
					const array = reader.readExactly(lengthOrField);
					return converter.convert(yield* then(array));
				};
				init = (value) => converter.back(value);
			} else deserialize = function* (_then, reader) {
				return reader.readExactly(lengthOrField);
			};
		}
		return field(lengthOrField, "byob", serialize, deserialize, { init });
	}
	if ((typeof lengthOrField === "object" || typeof lengthOrField === "function") && "serialize" in lengthOrField) {
		let deserialize;
		let init;
		if (converter) {
			deserialize = function* (then, reader, context) {
				const length = yield* then(lengthOrField.deserialize(reader, context));
				const array = length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
				return converter.convert(yield* then(array));
			};
			init = (value) => converter.back(value);
		} else deserialize = function* (then, reader, context) {
			const length = yield* then(lengthOrField.deserialize(reader, context));
			return length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
		};
		return field(lengthOrField.size, "default", (value, { littleEndian }) => {
			if (lengthOrField.type === "default") {
				const lengthBuffer = lengthOrField.serialize(value.length, { littleEndian });
				if (value.length === 0) return lengthBuffer;
				const result = new Uint8Array(lengthBuffer.length + value.length);
				result.set(lengthBuffer, 0);
				result.set(value, lengthBuffer.length);
				return result;
			} else {
				const result = new Uint8Array(lengthOrField.size + value.length);
				lengthOrField.serialize(value.length, {
					buffer: result,
					index: 0,
					littleEndian
				});
				result.set(value, lengthOrField.size);
				return result;
			}
		}, deserialize, { init });
	}
	if (typeof lengthOrField === "string") {
		let deserialize;
		let init;
		if (converter) {
			deserialize = function* (then, reader, { dependencies }) {
				const length = dependencies[lengthOrField];
				const array = length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
				return converter.convert(yield* then(array));
			};
			init = (value, dependencies) => {
				const array = converter.back(value);
				dependencies[lengthOrField] = array.length;
				return array;
			};
		} else {
			deserialize = function* (_then, reader, { dependencies }) {
				const length = dependencies[lengthOrField];
				return length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
			};
			init = (value, dependencies) => {
				const array = value;
				dependencies[lengthOrField] = array.length;
				return array;
			};
		}
		return field(0, "default", (source) => source, deserialize, { init });
	}
	let deserialize;
	let init;
	if (converter) {
		deserialize = function* (then, reader, { dependencies }) {
			const rawLength = dependencies[lengthOrField.field];
			const length = lengthOrField.convert(rawLength);
			const array = length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
			return converter.convert(yield* then(array));
		};
		init = (value, dependencies) => {
			const array = converter.back(value);
			dependencies[lengthOrField.field] = lengthOrField.back(array.length);
			return array;
		};
	} else {
		deserialize = function* (_then, reader, { dependencies }) {
			const rawLength = dependencies[lengthOrField.field];
			const length = lengthOrField.convert(rawLength);
			return length !== 0 ? reader.readExactly(length) : EmptyUint8Array;
		};
		init = (value, dependencies) => {
			const array = value;
			dependencies[lengthOrField.field] = lengthOrField.back(array.length);
			return array;
		};
	}
	return field(0, "default", (source) => source, deserialize, { init });
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/readable.js
var ExactReadableEndedError = class extends Error {
	constructor() {
		super("ExactReadable ended");
	}
};
var Uint8ArrayExactReadable = class {
	#data;
	#position;
	get position() {
		return this.#position;
	}
	constructor(data) {
		this.#data = data;
		this.#position = 0;
	}
	readExactly(length) {
		if (this.#position + length > this.#data.length) throw new ExactReadableEndedError();
		const result = this.#data.subarray(this.#position, this.#position + length);
		this.#position += length;
		return result;
	}
};
//#endregion
//#region node_modules/@yume-chan/struct/esm/struct.js
var StructDeserializeError = class extends Error {
	constructor(message) {
		super(message);
	}
};
var StructNotEnoughDataError = class extends StructDeserializeError {
	constructor() {
		super("The underlying readable was ended before the struct was fully deserialized");
	}
};
var StructEmptyError = class extends StructDeserializeError {
	constructor() {
		super("The underlying readable doesn't contain any more struct");
	}
};
/* @__NO_SIDE_EFFECTS__ */
function struct(fields, options) {
	const fieldList = Object.entries(fields);
	let size = 0;
	let byob = true;
	for (const [, field] of fieldList) {
		size += field.size;
		if (byob && field.type !== "byob") byob = false;
	}
	const littleEndian = options.littleEndian;
	const extra = options.extra ? Object.getOwnPropertyDescriptors(options.extra) : void 0;
	return {
		littleEndian,
		fields,
		extra: options.extra,
		type: byob ? "byob" : "default",
		size,
		serialize(source, bufferOrContext) {
			const temp = { ...source };
			for (const [key, field] of fieldList) if (key in temp && "init" in field) temp[key] = field.init?.(temp[key], temp);
			const sizes = new Array(fieldList.length);
			const buffers = new Array(fieldList.length);
			{
				const context = { littleEndian };
				for (const [index, [key, field]] of fieldList.entries()) if (field.type === "byob") sizes[index] = field.size;
				else {
					buffers[index] = field.serialize(temp[key], context);
					sizes[index] = buffers[index].length;
				}
			}
			const size = sizes.reduce((sum, size) => sum + size, 0);
			let externalBuffer;
			let buffer;
			let index;
			if (bufferOrContext instanceof Uint8Array) {
				if (bufferOrContext.length < size) throw new Error("Buffer too small");
				externalBuffer = true;
				buffer = bufferOrContext;
				index = 0;
			} else if (typeof bufferOrContext === "object" && "buffer" in bufferOrContext) {
				externalBuffer = true;
				buffer = bufferOrContext.buffer;
				index = bufferOrContext.index ?? 0;
				if (buffer.length - index < size) throw new Error("Buffer too small");
			} else {
				externalBuffer = false;
				buffer = new Uint8Array(size);
				index = 0;
			}
			const context = {
				buffer,
				index,
				littleEndian
			};
			for (const [index, [key, field]] of fieldList.entries()) {
				if (buffers[index]) buffer.set(buffers[index], context.index);
				else field.serialize(temp[key], context);
				context.index += sizes[index];
			}
			if (externalBuffer) return size;
			else return buffer;
		},
		deserialize: /* @__PURE__ */ bipedal(function* (then, reader) {
			const startPosition = reader.position;
			const result = {};
			const context = {
				dependencies: result,
				littleEndian
			};
			try {
				for (const [key, field] of fieldList) result[key] = yield* then(field.deserialize(reader, context));
			} catch (e) {
				if (!(e instanceof ExactReadableEndedError)) throw e;
				if (reader.position === startPosition) throw new StructEmptyError();
				else throw new StructNotEnoughDataError();
			}
			if (extra) Object.defineProperties(result, extra);
			if (options.postDeserialize) return options.postDeserialize.call(result, result);
			else return result;
		})
	};
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/extend.js
/* @__NO_SIDE_EFFECTS__ */
function extend(base, fields, options) {
	return /* @__PURE__ */ struct(Object.assign({}, base.fields, fields), {
		littleEndian: options?.littleEndian ?? base.littleEndian,
		extra: base.extra,
		postDeserialize: options?.postDeserialize
	});
}
//#endregion
//#region node_modules/@yume-chan/no-data-view/esm/int32.js
/* @__NO_SIDE_EFFECTS__ */
function getInt32(buffer, offset, littleEndian) {
	return littleEndian ? buffer[offset] | buffer[offset + 1] << 8 | buffer[offset + 2] << 16 | buffer[offset + 3] << 24 : buffer[offset] << 24 | buffer[offset + 1] << 16 | buffer[offset + 2] << 8 | buffer[offset + 3];
}
function setInt32(buffer, offset, value, littleEndian) {
	if (littleEndian) {
		buffer[offset] = value;
		buffer[offset + 1] = value >> 8;
		buffer[offset + 2] = value >> 16;
		buffer[offset + 3] = value >> 24;
	} else {
		buffer[offset] = value >> 24;
		buffer[offset + 1] = value >> 16;
		buffer[offset + 2] = value >> 8;
		buffer[offset + 3] = value;
	}
}
//#endregion
//#region node_modules/@yume-chan/no-data-view/esm/int64.js
function setInt64LittleEndian(buffer, offset, value) {
	buffer[offset] = Number(value & 255n);
	buffer[offset + 1] = Number(value >> 8n & 255n);
	buffer[offset + 2] = Number(value >> 16n & 255n);
	buffer[offset + 3] = Number(value >> 24n & 255n);
	buffer[offset + 4] = Number(value >> 32n & 255n);
	buffer[offset + 5] = Number(value >> 40n & 255n);
	buffer[offset + 6] = Number(value >> 48n & 255n);
	buffer[offset + 7] = Number(value >> 56n & 255n);
}
function setInt64BigEndian(buffer, offset, value) {
	buffer[offset] = Number(value >> 56n & 255n);
	buffer[offset + 1] = Number(value >> 48n & 255n);
	buffer[offset + 2] = Number(value >> 40n & 255n);
	buffer[offset + 3] = Number(value >> 32n & 255n);
	buffer[offset + 4] = Number(value >> 24n & 255n);
	buffer[offset + 5] = Number(value >> 16n & 255n);
	buffer[offset + 6] = Number(value >> 8n & 255n);
	buffer[offset + 7] = Number(value & 255n);
}
//#endregion
//#region node_modules/@yume-chan/no-data-view/esm/uint32.js
/* @__NO_SIDE_EFFECTS__ */
function getUint32LittleEndian(buffer, offset) {
	return (buffer[offset] | buffer[offset + 1] << 8 | buffer[offset + 2] << 16 | buffer[offset + 3] << 24) >>> 0;
}
/* @__NO_SIDE_EFFECTS__ */
function getUint32(buffer, offset, littleEndian) {
	return littleEndian ? (buffer[offset] | buffer[offset + 1] << 8 | buffer[offset + 2] << 16 | buffer[offset + 3] << 24) >>> 0 : (buffer[offset] << 24 | buffer[offset + 1] << 16 | buffer[offset + 2] << 8 | buffer[offset + 3]) >>> 0;
}
function setUint32LittleEndian(buffer, offset, value) {
	buffer[offset] = value;
	buffer[offset + 1] = value >> 8;
	buffer[offset + 2] = value >> 16;
	buffer[offset + 3] = value >> 24;
}
function setUint32(buffer, offset, value, littleEndian) {
	if (littleEndian) {
		buffer[offset] = value;
		buffer[offset + 1] = value >> 8;
		buffer[offset + 2] = value >> 16;
		buffer[offset + 3] = value >> 24;
	} else {
		buffer[offset] = value >> 24;
		buffer[offset + 1] = value >> 16;
		buffer[offset + 2] = value >> 8;
		buffer[offset + 3] = value;
	}
}
//#endregion
//#region node_modules/@yume-chan/no-data-view/esm/uint64.js
function getUint64LittleEndian(buffer, offset) {
	return BigInt(buffer[offset]) | BigInt(buffer[offset + 1]) << 8n | BigInt(buffer[offset + 2]) << 16n | BigInt(buffer[offset + 3]) << 24n | BigInt(buffer[offset + 4]) << 32n | BigInt(buffer[offset + 5]) << 40n | BigInt(buffer[offset + 6]) << 48n | BigInt(buffer[offset + 7]) << 56n;
}
function getUint64BigEndian(buffer, offset) {
	return BigInt(buffer[offset]) << 56n | BigInt(buffer[offset + 1]) << 48n | BigInt(buffer[offset + 2]) << 40n | BigInt(buffer[offset + 3]) << 32n | BigInt(buffer[offset + 4]) << 24n | BigInt(buffer[offset + 5]) << 16n | BigInt(buffer[offset + 6]) << 8n | BigInt(buffer[offset + 7]);
}
function getUint64(buffer, offset, littleEndian) {
	return littleEndian ? BigInt(buffer[offset]) | BigInt(buffer[offset + 1]) << 8n | BigInt(buffer[offset + 2]) << 16n | BigInt(buffer[offset + 3]) << 24n | BigInt(buffer[offset + 4]) << 32n | BigInt(buffer[offset + 5]) << 40n | BigInt(buffer[offset + 6]) << 48n | BigInt(buffer[offset + 7]) << 56n : BigInt(buffer[offset]) << 56n | BigInt(buffer[offset + 1]) << 48n | BigInt(buffer[offset + 2]) << 40n | BigInt(buffer[offset + 3]) << 32n | BigInt(buffer[offset + 4]) << 24n | BigInt(buffer[offset + 5]) << 16n | BigInt(buffer[offset + 6]) << 8n | BigInt(buffer[offset + 7]);
}
function setUint64(buffer, offset, value, littleEndian) {
	if (littleEndian) {
		buffer[offset] = Number(value & 255n);
		buffer[offset + 1] = Number(value >> 8n & 255n);
		buffer[offset + 2] = Number(value >> 16n & 255n);
		buffer[offset + 3] = Number(value >> 24n & 255n);
		buffer[offset + 4] = Number(value >> 32n & 255n);
		buffer[offset + 5] = Number(value >> 40n & 255n);
		buffer[offset + 6] = Number(value >> 48n & 255n);
		buffer[offset + 7] = Number(value >> 56n & 255n);
	} else {
		buffer[offset] = Number(value >> 56n & 255n);
		buffer[offset + 1] = Number(value >> 48n & 255n);
		buffer[offset + 2] = Number(value >> 40n & 255n);
		buffer[offset + 3] = Number(value >> 32n & 255n);
		buffer[offset + 4] = Number(value >> 24n & 255n);
		buffer[offset + 5] = Number(value >> 16n & 255n);
		buffer[offset + 6] = Number(value >> 8n & 255n);
		buffer[offset + 7] = Number(value & 255n);
	}
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/number.js
/* @__NO_SIDE_EFFECTS__ */
function number(size, serialize, deserialize) {
	const fn = (() => fn);
	Object.assign(fn, field(size, "byob", serialize, deserialize));
	return fn;
}
var u8 = /* @__PURE__ */ number(1, (value, { buffer, index }) => {
	buffer[index] = value;
}, function* (then, reader) {
	return (yield* then(reader.readExactly(1)))[0];
});
var u32 = /* @__PURE__ */ number(4, (value, { buffer, index, littleEndian }) => {
	setUint32(buffer, index, value, littleEndian);
}, function* (then, reader, { littleEndian }) {
	return /* @__PURE__ */ getUint32(yield* then(reader.readExactly(4)), 0, littleEndian);
});
var s32 = /* @__PURE__ */ number(4, (value, { buffer, index, littleEndian }) => {
	setInt32(buffer, index, value, littleEndian);
}, function* (then, reader, { littleEndian }) {
	return /* @__PURE__ */ getInt32(yield* then(reader.readExactly(4)), 0, littleEndian);
});
var u64 = /* @__PURE__ */ number(8, (value, { buffer, index, littleEndian }) => {
	setUint64(buffer, index, value, littleEndian);
}, function* (then, reader, { littleEndian }) {
	return getUint64(yield* then(reader.readExactly(8)), 0, littleEndian);
});
//#endregion
//#region node_modules/@yume-chan/struct/esm/utils.js
var { TextEncoder, TextDecoder } = globalThis;
var SharedEncoder = /* @__PURE__ */ new TextEncoder();
var SharedDecoder = /* @__PURE__ */ new TextDecoder();
/* @__NO_SIDE_EFFECTS__ */
function encodeUtf8(input) {
	return SharedEncoder.encode(input);
}
/* @__NO_SIDE_EFFECTS__ */
function decodeUtf8(buffer) {
	return SharedDecoder.decode(buffer);
}
//#endregion
//#region node_modules/@yume-chan/struct/esm/string.js
var string = /* @__NO_SIDE_EFFECTS__ */ ((lengthOrField) => {
	const field = /* @__PURE__ */ buffer(lengthOrField, {
		convert: decodeUtf8,
		back: encodeUtf8
	});
	field.as = () => field;
	return field;
});
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/stream.js
var { AbortController } = globalThis;
var ReadableStream = /* @__PURE__ */ (() => {
	const { ReadableStream } = globalThis;
	if (!ReadableStream.from) ReadableStream.from = function(iterable) {
		const iterator = Symbol.asyncIterator in iterable ? iterable[Symbol.asyncIterator]() : iterable[Symbol.iterator]();
		return new ReadableStream({
			async pull(controller) {
				const result = await iterator.next();
				if (result.done) {
					controller.close();
					return;
				}
				controller.enqueue(result.value);
			},
			async cancel(reason) {
				await iterator.return?.(reason);
			}
		});
	};
	if (!ReadableStream.prototype[Symbol.asyncIterator] || !ReadableStream.prototype.values) {
		ReadableStream.prototype.values = async function* (options) {
			const reader = this.getReader();
			try {
				while (true) {
					const { done, value } = await reader.read();
					if (done) return;
					yield value;
				}
			} finally {
				if (!options?.preventCancel) await reader.cancel();
				reader.releaseLock();
			}
		};
		ReadableStream.prototype[Symbol.asyncIterator] = ReadableStream.prototype.values;
	}
	return ReadableStream;
})();
var { WritableStream, TransformStream } = globalThis;
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/push-readable.js
var PushReadableStream = class extends ReadableStream {
	/**
	* Create a new `PushReadableStream` from a source.
	*
	* @param source If `source` returns a `Promise`, the stream will be closed
	* when the `Promise` is resolved, and be errored when the `Promise` is rejected.
	* @param strategy
	*/
	constructor(source, strategy, logger) {
		let waterMarkLow;
		let zeroHighWaterMarkAllowEnqueue = false;
		const abortController = new AbortController();
		super({
			start: (controller) => {
				const result = source({
					abortSignal: abortController.signal,
					enqueue: async (chunk) => {
						logger?.({
							source: "producer",
							operation: "enqueue",
							value: chunk,
							phase: "start"
						});
						if (abortController.signal.aborted) {
							logger?.({
								source: "producer",
								operation: "enqueue",
								value: chunk,
								phase: "ignored"
							});
							return;
						}
						if (controller.desiredSize === null) {
							controller.enqueue(chunk);
							// istanbul ignore next
							return;
						}
						if (zeroHighWaterMarkAllowEnqueue) {
							zeroHighWaterMarkAllowEnqueue = false;
							controller.enqueue(chunk);
							logger?.({
								source: "producer",
								operation: "enqueue",
								value: chunk,
								phase: "complete"
							});
							return;
						}
						if (controller.desiredSize <= 0) {
							logger?.({
								source: "producer",
								operation: "enqueue",
								value: chunk,
								phase: "waiting"
							});
							waterMarkLow = new PromiseResolver();
							await waterMarkLow.promise;
							if (abortController.signal.aborted) {
								logger?.({
									source: "producer",
									operation: "enqueue",
									value: chunk,
									phase: "ignored"
								});
								return;
							}
						}
						controller.enqueue(chunk);
						logger?.({
							source: "producer",
							operation: "enqueue",
							value: chunk,
							phase: "complete"
						});
					},
					close() {
						logger?.({
							source: "producer",
							operation: "close",
							explicit: true,
							phase: "start"
						});
						if (abortController.signal.aborted) {
							logger?.({
								source: "producer",
								operation: "close",
								explicit: true,
								phase: "ignored"
							});
							return;
						}
						controller.close();
						logger?.({
							source: "producer",
							operation: "close",
							explicit: true,
							phase: "complete"
						});
					},
					error(e) {
						logger?.({
							source: "producer",
							operation: "error",
							explicit: true,
							phase: "start"
						});
						controller.error(e);
						logger?.({
							source: "producer",
							operation: "error",
							explicit: true,
							phase: "complete"
						});
					}
				});
				if (result && "then" in result) result.then(() => {
					logger?.({
						source: "producer",
						operation: "close",
						explicit: false,
						phase: "start"
					});
					try {
						controller.close();
						logger?.({
							source: "producer",
							operation: "close",
							explicit: false,
							phase: "complete"
						});
					} catch {
						logger?.({
							source: "producer",
							operation: "close",
							explicit: false,
							phase: "ignored"
						});
					}
				}, (e) => {
					logger?.({
						source: "producer",
						operation: "error",
						explicit: false,
						phase: "start"
					});
					controller.error(e);
					logger?.({
						source: "producer",
						operation: "error",
						explicit: false,
						phase: "complete"
					});
				});
			},
			pull: () => {
				logger?.({
					source: "consumer",
					operation: "pull",
					phase: "start"
				});
				if (waterMarkLow) waterMarkLow.resolve();
				else if (strategy?.highWaterMark === 0) zeroHighWaterMarkAllowEnqueue = true;
				logger?.({
					source: "consumer",
					operation: "pull",
					phase: "complete"
				});
			},
			cancel: (reason) => {
				logger?.({
					source: "consumer",
					operation: "cancel",
					phase: "start"
				});
				abortController.abort(reason);
				waterMarkLow?.resolve();
				logger?.({
					source: "consumer",
					operation: "cancel",
					phase: "complete"
				});
			}
		}, strategy);
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/try-close.js
function tryClose(value) {
	try {
		const result = value.close();
		if (isPromiseLike(result)) return result.then(() => true, () => false);
		return true;
	} catch {
		return false;
	}
}
async function tryCancel(stream) {
	try {
		await stream.cancel();
		return true;
	} catch {
		return false;
	}
}
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/buffered.js
var BufferedReadableStream = class {
	#buffered;
	#bufferedOffset = 0;
	#bufferedLength = 0;
	#position = 0;
	get position() {
		return this.#position;
	}
	stream;
	reader;
	constructor(stream) {
		this.stream = stream;
		this.reader = stream.getReader();
	}
	#readBuffered(length) {
		if (!this.#buffered) return;
		const value = this.#buffered.subarray(this.#bufferedOffset, this.#bufferedOffset + length);
		if (this.#bufferedLength > length) {
			this.#position += length;
			this.#bufferedOffset += length;
			this.#bufferedLength -= length;
			return value;
		}
		this.#position += this.#bufferedLength;
		this.#buffered = void 0;
		this.#bufferedOffset = 0;
		this.#bufferedLength = 0;
		return value;
	}
	async #readSource(length) {
		const { done, value } = await this.reader.read();
		if (done) throw new ExactReadableEndedError();
		if (value.length > length) {
			this.#buffered = value;
			this.#bufferedOffset = length;
			this.#bufferedLength = value.length - length;
			this.#position += length;
			return value.subarray(0, length);
		}
		this.#position += value.length;
		return value;
	}
	iterateExactly(length) {
		let state = this.#buffered ? 0 : 1;
		return { next: () => {
			switch (state) {
				case 0: {
					const value = this.#readBuffered(length);
					if (value.length === length) state = 2;
					else {
						length -= value.length;
						state = 1;
					}
					return {
						done: false,
						value
					};
				}
				case 1:
					state = 3;
					return {
						done: false,
						value: this.#readSource(length).then((value) => {
							if (value.length === length) state = 2;
							else {
								length -= value.length;
								state = 1;
							}
							return value;
						})
					};
				case 2: return {
					done: true,
					value: void 0
				};
				case 3: throw new Error("Can't call `next` before previous Promise resolves");
				default: throw new Error("unreachable");
			}
		} };
	}
	readExactly = /* @__PURE__ */ bipedal(function* (then, length) {
		let result;
		let index = 0;
		const initial = this.#readBuffered(length);
		if (initial) {
			if (initial.length === length) return initial;
			result = new Uint8Array(length);
			result.set(initial, index);
			index += initial.length;
			length -= initial.length;
		} else result = new Uint8Array(length);
		while (length > 0) {
			const value = yield* then(this.#readSource(length));
			result.set(value, index);
			index += value.length;
			length -= value.length;
		}
		return result;
	});
	/**
	* Return a readable stream with unconsumed data (if any) and
	* all data from the wrapped stream.
	* @returns A `ReadableStream`
	*/
	release() {
		if (this.#bufferedLength > 0) return new PushReadableStream(async (controller) => {
			const buffered = this.#buffered.subarray(this.#bufferedOffset);
			await controller.enqueue(buffered);
			controller.abortSignal.addEventListener("abort", () => {
				tryCancel(this.reader);
			});
			while (true) {
				const { done, value } = await this.reader.read();
				if (done) return;
				await controller.enqueue(value);
			}
		});
		else {
			this.reader.releaseLock();
			return this.stream;
		}
	}
	async cancel(reason) {
		await this.reader.cancel(reason);
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/buffered-transform.js
var BufferedTransformStream = class {
	#readable;
	get readable() {
		return this.#readable;
	}
	#writable;
	get writable() {
		return this.#writable;
	}
	constructor(transform) {
		let bufferedStreamController;
		let writableStreamController;
		const buffered = new BufferedReadableStream(new PushReadableStream((controller) => {
			bufferedStreamController = controller;
		}));
		this.#readable = new ReadableStream({
			async pull(controller) {
				try {
					const value = await transform(buffered);
					controller.enqueue(value);
				} catch (e) {
					if (e instanceof StructEmptyError) {
						controller.close();
						return;
					}
					throw e;
				}
			},
			cancel: (reason) => {
				return writableStreamController.error(reason);
			}
		});
		this.#writable = new WritableStream({
			start(controller) {
				writableStreamController = controller;
			},
			async write(chunk) {
				await bufferedStreamController.enqueue(chunk);
			},
			abort() {
				bufferedStreamController.close();
			},
			close() {
				bufferedStreamController.close();
			}
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/concat.js
/**
* A `TransformStream` that concatenates strings.
*
* Its `readable` is also a `Promise<string>`, so it's possible to `await` it to get the result.
*
* ```ts
* const result: string = await readable.pipeThrough(new ConcatStringStream());
* ```
*/
var ConcatStringStream = class {
	#result = "";
	#resolver = new PromiseResolver();
	#writable = new WritableStream({
		write: (chunk) => {
			this.#result += chunk;
		},
		close: () => {
			this.#resolver.resolve(this.#result);
			this.#readableController.enqueue(this.#result);
			this.#readableController.close();
		},
		abort: (reason) => {
			this.#resolver.reject(reason);
			this.#readableController.error(reason);
		}
	});
	get writable() {
		return this.#writable;
	}
	#readableController;
	#readable = new ReadableStream({ start: (controller) => {
		this.#readableController = controller;
	} });
	get readable() {
		return this.#readable;
	}
	constructor() {
		Object.defineProperties(this.#readable, {
			then: { get: () => this.#resolver.promise.then.bind(this.#resolver.promise) },
			catch: { get: () => this.#resolver.promise.catch.bind(this.#resolver.promise) },
			finally: { get: () => this.#resolver.promise.finally.bind(this.#resolver.promise) }
		});
	}
};
/**
* A `TransformStream` that concatenates `Uint8Array`s.
*
* If you want to decode the result as string,
* prefer `.pipeThrough(new TextDecoderStream()).pipeThrough(new ConcatStringStream())`,
* than `.pipeThough(new ConcatBufferStream()).pipeThrough(new TextDecoderStream())`,
* because of JavaScript engine optimizations,
* concatenating strings is faster than concatenating `Uint8Array`s.
*/
var ConcatBufferStream = class {
	#segments = [];
	#resolver = new PromiseResolver();
	#writable = new WritableStream({
		write: (chunk) => {
			this.#segments.push(chunk);
		},
		close: () => {
			let result;
			let offset = 0;
			switch (this.#segments.length) {
				case 0:
					result = EmptyUint8Array;
					break;
				case 1:
					result = this.#segments[0];
					break;
				default:
					result = new Uint8Array(this.#segments.reduce((prev, item) => prev + item.length, 0));
					for (const segment of this.#segments) {
						result.set(segment, offset);
						offset += segment.length;
					}
					break;
			}
			this.#resolver.resolve(result);
			this.#readableController.enqueue(result);
			this.#readableController.close();
		},
		abort: (reason) => {
			this.#resolver.reject(reason);
			this.#readableController.error(reason);
		}
	});
	get writable() {
		return this.#writable;
	}
	#readableController;
	#readable = new ReadableStream({ start: (controller) => {
		this.#readableController = controller;
	} });
	get readable() {
		return this.#readable;
	}
	constructor() {
		Object.defineProperties(this.#readable, {
			then: { get: () => this.#resolver.promise.then.bind(this.#resolver.promise) },
			catch: { get: () => this.#resolver.promise.catch.bind(this.#resolver.promise) },
			finally: { get: () => this.#resolver.promise.finally.bind(this.#resolver.promise) }
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/consumable/readable.js
var ConsumableReadableStream = class ConsumableReadableStream extends ReadableStream {
	static async enqueue(controller, chunk) {
		const output = new Consumable(chunk);
		controller.enqueue(output);
		await output.consumed;
	}
	constructor(source, strategy) {
		let wrappedController;
		let wrappedStrategy;
		if (strategy) {
			wrappedStrategy = {};
			if ("highWaterMark" in strategy) wrappedStrategy.highWaterMark = strategy.highWaterMark;
			if ("size" in strategy) wrappedStrategy.size = (chunk) => {
				return strategy.size(chunk.value);
			};
		}
		super({
			start(controller) {
				wrappedController = {
					enqueue(chunk) {
						return ConsumableReadableStream.enqueue(controller, chunk);
					},
					close() {
						controller.close();
					},
					error(reason) {
						controller.error(reason);
					}
				};
				return source.start?.(wrappedController);
			},
			pull() {
				return source.pull?.(wrappedController);
			},
			cancel(reason) {
				return source.cancel?.(reason);
			}
		}, wrappedStrategy);
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/consumable/wrap-byte-readable.js
var ConsumableWrapByteReadableStream = class extends ReadableStream {
	constructor(stream, chunkSize, min) {
		const reader = stream.getReader({ mode: "byob" });
		let array = new Uint8Array(chunkSize);
		super({
			async pull(controller) {
				const { done, value } = await reader.read(array, { min });
				if (done) {
					controller.close();
					return;
				}
				await ConsumableReadableStream.enqueue(controller, value);
				array = new Uint8Array(value.buffer);
			},
			cancel(reason) {
				return reader.cancel(reason);
			}
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/consumable/wrap-writable.js
var ConsumableWrapWritableStream = class extends WritableStream {
	constructor(stream) {
		const writer = stream.getWriter();
		super({
			write(chunk) {
				return chunk.tryConsume((chunk) => writer.write(chunk));
			},
			abort(reason) {
				return writer.abort(reason);
			},
			close() {
				return writer.close();
			}
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/consumable/writable.js
var ConsumableWritableStream = class extends WritableStream {
	static async write(writer, value) {
		const consumable = new Consumable(value);
		await writer.write(consumable);
		await consumable.consumed;
	}
	constructor(sink, strategy) {
		let wrappedStrategy;
		if (strategy) {
			wrappedStrategy = {};
			if ("highWaterMark" in strategy) wrappedStrategy.highWaterMark = strategy.highWaterMark;
			if ("size" in strategy) wrappedStrategy.size = (chunk) => {
				return strategy.size(chunk instanceof Consumable ? chunk.value : chunk);
			};
		}
		super({
			start(controller) {
				return sink.start?.(controller);
			},
			write(chunk, controller) {
				return chunk.tryConsume((chunk) => sink.write?.(chunk, controller));
			},
			abort(reason) {
				return sink.abort?.(reason);
			},
			close() {
				return sink.close?.();
			}
		}, wrappedStrategy);
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/task.js
var { console } = globalThis;
var createTask = /* @__PURE__ */ (() => console?.createTask?.bind(console) ?? (() => ({ run(callback) {
	return callback();
} })))();
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/consumable.js
var Consumable = class {
	static WritableStream = ConsumableWritableStream;
	static WrapWritableStream = ConsumableWrapWritableStream;
	static ReadableStream = ConsumableReadableStream;
	static WrapByteReadableStream = ConsumableWrapByteReadableStream;
	#task;
	#resolver;
	value;
	consumed;
	constructor(value) {
		this.#task = createTask("Consumable");
		this.value = value;
		this.#resolver = new PromiseResolver();
		this.consumed = this.#resolver.promise;
	}
	consume() {
		this.#resolver.resolve();
	}
	error(error) {
		this.#resolver.reject(error);
	}
	tryConsume(callback) {
		try {
			let result = this.#task.run(() => callback(this.value));
			if (isPromiseLike(result)) result = result.then((value) => {
				this.#resolver.resolve();
				return value;
			}, (e) => {
				this.#resolver.reject(e);
				throw e;
			});
			else this.#resolver.resolve();
			return result;
		} catch (e) {
			this.#resolver.reject(e);
			throw e;
		}
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/maybe-consumable/utils.js
function tryConsume(value, callback) {
	if (value instanceof Consumable) return value.tryConsume(callback);
	else return callback(value);
}
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/maybe-consumable/writable.js
var MaybeConsumableWritableStream = class extends WritableStream {
	constructor(sink, strategy) {
		let wrappedStrategy;
		if (strategy) {
			wrappedStrategy = {};
			if ("highWaterMark" in strategy) wrappedStrategy.highWaterMark = strategy.highWaterMark;
			if ("size" in strategy) wrappedStrategy.size = (chunk) => {
				return strategy.size(chunk instanceof Consumable ? chunk.value : chunk);
			};
		}
		super({
			start(controller) {
				return sink.start?.(controller);
			},
			write(chunk, controller) {
				return tryConsume(chunk, (chunk) => sink.write?.(chunk, controller));
			},
			abort(reason) {
				return sink.abort?.(reason);
			},
			close() {
				return sink.close?.();
			}
		}, wrappedStrategy);
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/distribution.js
/**
* Splits or combines buffers to specified size.
*/
var BufferCombiner = class {
	#capacity;
	#buffer;
	#offset;
	#available;
	constructor(size) {
		this.#capacity = size;
		this.#buffer = new Uint8Array(size);
		this.#offset = 0;
		this.#available = size;
	}
	/**
	* Pushes data to the combiner.
	* @param data The input data to be split or combined.
	* @returns
	* A generator that yields buffers of specified size.
	* It may yield the same buffer multiple times, consume the data before calling `next`.
	*/
	*push(data) {
		let offset = 0;
		let available = data.length;
		if (this.#offset !== 0) if (available >= this.#available) {
			this.#buffer.set(data.subarray(0, this.#available), this.#offset);
			offset += this.#available;
			available -= this.#available;
			yield this.#buffer;
			this.#offset = 0;
			this.#available = this.#capacity;
			if (available === 0) return;
		} else {
			this.#buffer.set(data, this.#offset);
			this.#offset += available;
			this.#available -= available;
			return;
		}
		while (available >= this.#capacity) {
			const end = offset + this.#capacity;
			yield data.subarray(offset, end);
			offset = end;
			available -= this.#capacity;
		}
		if (available > 0) {
			this.#buffer.set(data.subarray(offset), this.#offset);
			this.#offset += available;
			this.#available -= available;
		}
	}
	flush() {
		if (this.#offset === 0) return;
		const output = this.#buffer.subarray(0, this.#offset);
		this.#offset = 0;
		this.#available = this.#capacity;
		return output;
	}
};
var DistributionStream = class extends TransformStream {
	constructor(size, combine = false) {
		const combiner = combine ? new BufferCombiner(size) : void 0;
		super({
			async transform(chunk, controller) {
				await tryConsume(chunk, async (chunk) => {
					if (combiner) for (const buffer of combiner.push(chunk)) await Consumable.ReadableStream.enqueue(controller, buffer);
					else {
						let offset = 0;
						let available = chunk.length;
						while (available > 0) {
							const end = offset + size;
							await Consumable.ReadableStream.enqueue(controller, chunk.subarray(offset, end));
							offset = end;
							available -= size;
						}
					}
				});
			},
			flush(controller) {
				if (combiner) {
					const data = combiner.flush();
					if (data) controller.enqueue(data);
				}
			}
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/encoding.js
var Global = globalThis;
var TextDecoderStream = Global.TextDecoderStream;
Global.TextEncoderStream;
//#endregion
//#region node_modules/@yume-chan/stream-extra/esm/struct-deserialize.js
var StructDeserializeStream = class extends BufferedTransformStream {
	constructor(struct) {
		super((stream) => {
			return struct.deserialize(stream);
		});
	}
};
//#endregion
//#region node_modules/@yume-chan/event/esm/disposable.js
var AutoDisposable = class {
	#disposables = [];
	constructor() {
		this.dispose = this.dispose.bind(this);
	}
	addDisposable(disposable) {
		this.#disposables.push(disposable);
		return disposable;
	}
	dispose() {
		for (const disposable of this.#disposables) disposable.dispose();
		this.#disposables = [];
	}
};
//#endregion
//#region node_modules/@yume-chan/event/esm/event-emitter.js
var EventEmitter = class {
	listeners = [];
	constructor() {
		this.event = this.event.bind(this);
	}
	addEventListener(info) {
		this.listeners.push(info);
		const remove = () => {
			const index = this.listeners.indexOf(info);
			if (index !== -1) this.listeners.splice(index, 1);
		};
		remove.dispose = remove;
		return remove;
	}
	event = (listener, thisArg, ...args) => {
		const info = {
			listener,
			thisArg,
			args
		};
		return this.addEventListener(info);
	};
	fire(e) {
		for (const info of this.listeners.slice()) info.listener.call(info.thisArg, e, ...info.args);
	}
	dispose() {
		this.listeners.length = 0;
	}
};
//#endregion
//#region node_modules/@yume-chan/event/esm/sticky-event-emitter.js
var Undefined = Symbol("undefined");
var StickyEventEmitter = class extends EventEmitter {
	#value = Undefined;
	addEventListener(info) {
		if (this.#value !== Undefined) info.listener.call(info.thisArg, this.#value, ...info.args);
		return super.addEventListener(info);
	}
	fire(e) {
		this.#value = e;
		super.fire(e);
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/base.js
var AdbServiceBase = class extends AutoDisposable {
	#adb;
	get adb() {
		return this.#adb;
	}
	constructor(adb) {
		super();
		this.#adb = adb;
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/framebuffer.js
var Version = /* @__PURE__ */ struct({ version: u32 }, { littleEndian: true });
var AdbFrameBufferV1 = /* @__PURE__ */ struct({
	bpp: u32,
	size: u32,
	width: u32,
	height: u32,
	red_offset: u32,
	red_length: u32,
	blue_offset: u32,
	blue_length: u32,
	green_offset: u32,
	green_length: u32,
	alpha_offset: u32,
	alpha_length: u32,
	data: /* @__PURE__ */ buffer("size")
}, { littleEndian: true });
var AdbFrameBufferV2 = /* @__PURE__ */ struct({
	bpp: u32,
	colorSpace: u32,
	size: u32,
	width: u32,
	height: u32,
	red_offset: u32,
	red_length: u32,
	blue_offset: u32,
	blue_length: u32,
	green_offset: u32,
	green_length: u32,
	alpha_offset: u32,
	alpha_length: u32,
	data: /* @__PURE__ */ buffer("size")
}, { littleEndian: true });
var AdbFrameBufferError = class extends Error {
	constructor(message, options) {
		super(message, options);
	}
};
var AdbFrameBufferUnsupportedVersionError = class extends AdbFrameBufferError {
	constructor(version) {
		super(`Unsupported FrameBuffer version ${version}`);
	}
};
var AdbFrameBufferForbiddenError = class extends AdbFrameBufferError {
	constructor() {
		super("FrameBuffer is disabled by current app");
	}
};
async function framebuffer(adb) {
	const stream = new BufferedReadableStream((await adb.createSocket("framebuffer:")).readable);
	let version;
	try {
		({version} = await Version.deserialize(stream));
	} catch (e) {
		if (e instanceof StructEmptyError) throw new AdbFrameBufferForbiddenError();
		throw e;
	}
	switch (version) {
		case 1: return await AdbFrameBufferV1.deserialize(stream);
		case 2: return await AdbFrameBufferV2.deserialize(stream);
		default: throw new AdbFrameBufferUnsupportedVersionError(version);
	}
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/power.js
var AdbPower = class extends AdbServiceBase {
	reboot(mode = "") {
		return this.adb.createSocketAndWait(`reboot:${mode}`);
	}
	bootloader() {
		return this.reboot("bootloader");
	}
	fastboot() {
		return this.reboot("fastboot");
	}
	recovery() {
		return this.reboot("recovery");
	}
	sideload() {
		return this.reboot("sideload");
	}
	/**
	* Reboot to Qualcomm Emergency Download (EDL) Mode.
	*
	* Only works on some Qualcomm devices.
	*/
	qualcommEdlMode() {
		return this.reboot("edl");
	}
	powerOff() {
		return this.adb.subprocess.noneProtocol.spawnWaitText(["reboot", "-p"]);
	}
	powerButton(longPress = false) {
		const args = ["input", "keyevent"];
		if (longPress) args.push("--longpress");
		args.push("POWER");
		return this.adb.subprocess.noneProtocol.spawnWaitText(args);
	}
	/**
	* Reboot to Samsung Odin download mode.
	*
	* Only works on Samsung devices.
	*/
	samsungOdin() {
		return this.reboot("download");
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/array-buffer.js
function toLocalUint8Array(value) {
	if (value.buffer instanceof ArrayBuffer) return value;
	const copy = new Uint8Array(value.length);
	copy.set(value);
	return copy;
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/auto-reset-event.js
var AutoResetEvent = class {
	#set;
	#queue = [];
	constructor(initialSet = false) {
		this.#set = initialSet;
	}
	wait() {
		if (!this.#set) {
			this.#set = true;
			if (this.#queue.length === 0) return Promise.resolve();
		}
		const resolver = new PromiseResolver();
		this.#queue.push(resolver);
		return resolver.promise;
	}
	notifyOne() {
		if (this.#queue.length !== 0) this.#queue.pop().resolve();
		else this.#set = false;
	}
	dispose() {
		for (const item of this.#queue) item.reject(/* @__PURE__ */ new Error("The AutoResetEvent has been disposed"));
		this.#queue.length = 0;
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/base64.js
var [charToIndex, indexToChar, paddingChar] = /* @__PURE__ */ (() => {
	const charToIndex = [];
	const indexToChar = [];
	const paddingChar = "=".charCodeAt(0);
	function addRange(start, end) {
		const charCodeStart = start.charCodeAt(0);
		const charCodeEnd = end.charCodeAt(0);
		for (let charCode = charCodeStart; charCode <= charCodeEnd; charCode += 1) {
			charToIndex[charCode] = indexToChar.length;
			indexToChar.push(charCode);
		}
	}
	addRange("A", "Z");
	addRange("a", "z");
	addRange("0", "9");
	addRange("+", "+");
	addRange("/", "/");
	return [
		charToIndex,
		indexToChar,
		paddingChar
	];
})();
/**
* Calculate the required length of the output buffer for the given input length.
*
* @param inputLength Length of the input in bytes
* @returns Length of the output in bytes
*/
function calculateBase64EncodedLength(inputLength) {
	const remainder = inputLength % 3;
	const paddingLength = remainder !== 0 ? 3 - remainder : 0;
	return [(inputLength + paddingLength) / 3 * 4, paddingLength];
}
function encodeBase64(input, output) {
	const [outputLength, paddingLength] = calculateBase64EncodedLength(input.length);
	if (!output) {
		output = new Uint8Array(outputLength);
		encodeForward(input, output, paddingLength);
		return output;
	} else {
		if (output.length < outputLength) throw new TypeError("output buffer is too small");
		output = output.subarray(0, outputLength);
		if (input.buffer !== output.buffer) encodeForward(input, output, paddingLength);
		else if (output.byteOffset + output.length - (paddingLength + 1) <= input.byteOffset + input.length) encodeForward(input, output, paddingLength);
		else if (output.byteOffset >= input.byteOffset - 1) encodeBackward(input, output, paddingLength);
		else throw new TypeError("input and output cannot overlap");
		return outputLength;
	}
}
function encodeForward(input, output, paddingLength) {
	let inputIndex = 0;
	let outputIndex = 0;
	while (inputIndex < input.length - 2) {
		const x = input[inputIndex];
		inputIndex += 1;
		const y = input[inputIndex];
		inputIndex += 1;
		const z = input[inputIndex];
		inputIndex += 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex += 1;
		output[outputIndex] = indexToChar[(x & 3) << 4 | y >> 4];
		outputIndex += 1;
		output[outputIndex] = indexToChar[(y & 15) << 2 | z >> 6];
		outputIndex += 1;
		output[outputIndex] = indexToChar[z & 63];
		outputIndex += 1;
	}
	if (paddingLength === 2) {
		const x = input[inputIndex];
		inputIndex += 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex += 1;
		output[outputIndex] = indexToChar[(x & 3) << 4];
		outputIndex += 1;
		output[outputIndex] = paddingChar;
		outputIndex += 1;
		output[outputIndex] = paddingChar;
	} else if (paddingLength === 1) {
		const x = input[inputIndex];
		inputIndex += 1;
		const y = input[inputIndex];
		inputIndex += 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex += 1;
		output[outputIndex] = indexToChar[(x & 3) << 4 | y >> 4];
		outputIndex += 1;
		output[outputIndex] = indexToChar[(y & 15) << 2];
		outputIndex += 1;
		output[outputIndex] = paddingChar;
	}
}
function encodeBackward(input, output, paddingLength) {
	let inputIndex = input.length - 1;
	let outputIndex = output.length - 1;
	if (paddingLength === 2) {
		const x = input[inputIndex];
		inputIndex -= 1;
		output[outputIndex] = paddingChar;
		outputIndex -= 1;
		output[outputIndex] = paddingChar;
		outputIndex -= 1;
		output[outputIndex] = indexToChar[(x & 3) << 4];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex -= 1;
	} else if (paddingLength === 1) {
		const y = input[inputIndex];
		inputIndex -= 1;
		const x = input[inputIndex];
		inputIndex -= 1;
		output[outputIndex] = paddingChar;
		outputIndex -= 1;
		output[outputIndex] = indexToChar[(y & 15) << 2];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[(x & 3) << 4 | y >> 4];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex -= 1;
	}
	while (inputIndex >= 0) {
		const z = input[inputIndex];
		inputIndex -= 1;
		const y = input[inputIndex];
		inputIndex -= 1;
		const x = input[inputIndex];
		inputIndex -= 1;
		output[outputIndex] = indexToChar[z & 63];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[(y & 15) << 2 | z >> 6];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[(x & 3) << 4 | y >> 4];
		outputIndex -= 1;
		output[outputIndex] = indexToChar[x >> 2];
		outputIndex -= 1;
	}
}
function decodeBase64(input) {
	let padding;
	if (input[input.length - 2] === "=") padding = 2;
	else if (input[input.length - 1] === "=") padding = 1;
	else padding = 0;
	const result = new Uint8Array(input.length / 4 * 3 - padding);
	let sIndex = 0;
	let dIndex = 0;
	while (sIndex < input.length - (padding !== 0 ? 4 : 0)) {
		const a = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const b = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const c = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const d = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		result[dIndex] = a << 2 | (b & 48) >> 4;
		dIndex += 1;
		result[dIndex] = (b & 15) << 4 | (c & 60) >> 2;
		dIndex += 1;
		result[dIndex] = (c & 3) << 6 | d;
		dIndex += 1;
	}
	if (padding === 1) {
		const a = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const b = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const c = charToIndex[input.charCodeAt(sIndex)];
		result[dIndex] = a << 2 | (b & 48) >> 4;
		dIndex += 1;
		result[dIndex] = (b & 15) << 4 | (c & 60) >> 2;
	} else if (padding === 2) {
		const a = charToIndex[input.charCodeAt(sIndex)];
		sIndex += 1;
		const b = charToIndex[input.charCodeAt(sIndex)];
		result[dIndex] = a << 2 | (b & 48) >> 4;
	}
	return result;
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/hex.js
function hexCharToNumber(char) {
	if (char < 48) throw new TypeError(`Invalid hex char ${char}`);
	if (char < 58) return char - 48;
	if (char < 65) throw new TypeError(`Invalid hex char ${char}`);
	if (char < 71) return char - 55;
	if (char < 97) throw new TypeError(`Invalid hex char ${char}`);
	if (char < 103) return char - 87;
	throw new TypeError(`Invalid hex char ${char}`);
}
function hexToNumber(data) {
	let result = 0;
	for (let i = 0; i < data.length; i += 1) result = result << 4 | hexCharToNumber(data[i]);
	return result;
}
function write4HexDigits(buffer, index, value) {
	const start = index;
	index += 3;
	while (index >= start && value > 0) {
		const digit = value & 15;
		value >>= 4;
		if (digit < 10) buffer[index] = digit + 48;
		else buffer[index] = digit + 87;
		index -= 1;
	}
	while (index >= start) {
		buffer[index] = 48;
		index -= 1;
	}
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/no-op.js
var NOOP = /* @__NO_SIDE_EFFECTS__ */ () => {};
/**
* When used in `Promise#catch`, means the promise should never throw errors.
* An explicit way to suppress ESLint floating promise warnings.
*/
function unreachable(...args) {
	throw new Error("Unreachable. Arguments:\n" + args.join("\n"));
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/ref.js
var { setInterval, clearInterval } = globalThis;
/**
* An object to keep current Node.js process alive even when no code is running.
*
* Does nothing in Web environments.
*
* Note that it does't have reference counting. Calling `unref` will
* remove the ref no matter how many times `ref` has been previously called, and vice versa.
* This is the same as how Node.js works.
*/
var Ref = class {
	#intervalId;
	constructor(options) {
		if (!options?.unref) this.ref();
	}
	ref() {
		this.#intervalId = setInterval(() => {}, 60 * 1e3);
	}
	unref() {
		if (this.#intervalId) {
			clearInterval(this.#intervalId);
			this.#intervalId = void 0;
		}
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/utils/sequence-equal.js
function sequenceEqual(a, b) {
	if (a.length !== b.length) return false;
	for (let i = 0; i < a.length; i += 1) if (a[i] !== b[i]) return false;
	return true;
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/reverse.js
var AdbReverseStringResponse = /* @__PURE__ */ struct({
	length: /* @__PURE__ */ string(4),
	content: /* @__PURE__ */ string({
		field: "length",
		convert(value) {
			return Number.parseInt(value, 16);
		},
		back(value) {
			return value.toString(16).padStart(4, "0");
		}
	})
}, { littleEndian: true });
var AdbReverseError = class extends Error {
	constructor(message) {
		super(message);
	}
};
var AdbReverseNotSupportedError = class extends AdbReverseError {
	constructor() {
		super("ADB reverse tunnel is not supported on this device when connected wirelessly.");
	}
};
var AdbReverseErrorResponse = /* @__PURE__ */ extend(AdbReverseStringResponse, {}, { postDeserialize(value) {
	if (value.content === "more than one device/emulator") throw new AdbReverseNotSupportedError();
	else throw new AdbReverseError(value.content);
} });
function decimalToNumber(buffer) {
	let value = 0;
	for (const byte of buffer) {
		if (byte < 48 || byte > 57) return value;
		value = value * 10 + byte - 48;
	}
	return value;
}
var OKAY$1 = /* @__PURE__ */ encodeUtf8("OKAY");
var AdbReverseService = class extends AdbServiceBase {
	#deviceAddressToLocalAddress = /* @__PURE__ */ new Map();
	async createBufferedStream(service) {
		return new BufferedReadableStream((await this.adb.createSocket(service)).readable);
	}
	async sendRequest(service) {
		const stream = await this.createBufferedStream(service);
		if (!sequenceEqual(await stream.readExactly(4), OKAY$1)) await AdbReverseErrorResponse.deserialize(stream);
		return stream;
	}
	/**
	* Get a list of all reverse port forwarding on the device.
	*/
	async list() {
		const stream = await this.createBufferedStream("reverse:list-forward");
		return (await AdbReverseStringResponse.deserialize(stream)).content.split("\n").filter((line) => !!line).map((line) => {
			const [deviceSerial, localName, remoteName] = line.split(" ");
			return {
				deviceSerial,
				localName,
				remoteName
			};
		});
	}
	/**
	* Add a reverse port forwarding for a program that already listens on a port.
	*/
	async addExternal(deviceAddress, localAddress) {
		const stream = await this.sendRequest(`reverse:forward:${deviceAddress};${localAddress}`);
		if (deviceAddress.startsWith("tcp:")) {
			const position = stream.position;
			try {
				const length = hexToNumber(await stream.readExactly(4));
				deviceAddress = `tcp:${decimalToNumber(await stream.readExactly(length))}`;
			} catch (e) {
				if (e instanceof ExactReadableEndedError && stream.position === position) {} else throw e;
			}
		}
		return deviceAddress;
	}
	/**
	* Add a reverse port forwarding.
	*/
	async add(deviceAddress, handler, localAddress) {
		localAddress = await this.adb.transport.addReverseTunnel(handler, localAddress);
		try {
			deviceAddress = await this.addExternal(deviceAddress, localAddress);
			this.#deviceAddressToLocalAddress.set(deviceAddress, localAddress);
			return deviceAddress;
		} catch (e) {
			await this.adb.transport.removeReverseTunnel(localAddress);
			throw e;
		}
	}
	/**
	* Remove a reverse port forwarding.
	*/
	async remove(deviceAddress) {
		const localAddress = this.#deviceAddressToLocalAddress.get(deviceAddress);
		if (localAddress) await this.adb.transport.removeReverseTunnel(localAddress);
		await this.sendRequest(`reverse:killforward:${deviceAddress}`);
	}
	/**
	* Remove all reverse port forwarding, including the ones added by other programs.
	*/
	async removeAll() {
		await this.adb.transport.clearReverseTunnels();
		this.#deviceAddressToLocalAddress.clear();
		await this.sendRequest(`reverse:killforward-all`);
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/none/process.js
var AdbNoneProtocolProcessImpl = class {
	#socket;
	get stdin() {
		return this.#socket.writable;
	}
	get output() {
		return this.#socket.readable;
	}
	#exited;
	get exited() {
		return this.#exited;
	}
	constructor(socket, signal) {
		this.#socket = socket;
		if (signal) {
			const exited = new PromiseResolver();
			this.#socket.closed.then(() => exited.resolve(void 0), (e) => exited.reject(e));
			signal.addEventListener("abort", () => {
				exited.reject(signal.reason);
				this.#socket.close();
			});
			this.#exited = exited.promise;
		} else this.#exited = this.#socket.closed;
	}
	kill() {
		return this.#socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/none/pty.js
var AdbNoneProtocolPtyProcess = class {
	#socket;
	#writer;
	#input;
	get input() {
		return this.#input;
	}
	get output() {
		return this.#socket.readable;
	}
	get exited() {
		return this.#socket.closed;
	}
	constructor(socket) {
		this.#socket = socket;
		this.#writer = this.#socket.writable.getWriter();
		this.#input = new MaybeConsumableWritableStream({ write: (chunk) => this.#writer.write(chunk) });
	}
	sigint() {
		return this.#writer.write(new Uint8Array([3]));
	}
	kill() {
		return this.#socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/utils.js
function escapeArg(s) {
	let result = "";
	result += `'`;
	let base = 0;
	while (true) {
		const found = s.indexOf(`'`, base);
		if (found === -1) {
			result += s.substring(base);
			break;
		}
		result += s.substring(base, found);
		result += String.raw`'\''`;
		base = found + 1;
	}
	result += `'`;
	return result;
}
function splitCommand(command) {
	const result = [];
	let quote;
	let isEscaped = false;
	let start = 0;
	for (let i = 0, len = command.length; i < len; i += 1) {
		if (isEscaped) {
			isEscaped = false;
			continue;
		}
		const char = command.charAt(i);
		switch (char) {
			case " ":
				if (!quote && i !== start) {
					result.push(command.substring(start, i));
					start = i + 1;
				}
				break;
			case "'":
			case "\"":
				if (!quote) quote = char;
				else if (char === quote) quote = void 0;
				break;
			case "\\":
				isEscaped = true;
				break;
		}
	}
	if (start < command.length) result.push(command.substring(start));
	return result;
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/none/spawner.js
var AdbNoneProtocolSpawner = class {
	#spawn;
	constructor(spawn) {
		this.#spawn = spawn;
	}
	spawn(command, signal) {
		signal?.throwIfAborted();
		if (typeof command === "string") command = splitCommand(command);
		return this.#spawn(command, signal);
	}
	async spawnWait(command) {
		return await (await this.spawn(command)).output.pipeThrough(new ConcatBufferStream());
	}
	async spawnWaitText(command) {
		return await (await this.spawn(command)).output.pipeThrough(new TextDecoderStream()).pipeThrough(new ConcatStringStream());
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/none/service.js
var AdbNoneProtocolSubprocessService = class extends AdbNoneProtocolSpawner {
	#adb;
	get adb() {
		return this.#adb;
	}
	constructor(adb) {
		super(async (command, signal) => {
			const socket = await this.#adb.createSocket(`exec:${command.join(" ")}`);
			if (signal?.aborted) {
				await socket.close();
				throw signal.reason;
			}
			return new AdbNoneProtocolProcessImpl(socket, signal);
		});
		this.#adb = adb;
	}
	async pty(command) {
		if (command === void 0) command = "";
		else if (Array.isArray(command)) command = command.join(" ");
		return new AdbNoneProtocolPtyProcess(await this.#adb.createSocket(`shell:${command}`));
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/features.js
var AdbFeature = {
	ShellV2: "shell_v2",
	Cmd: "cmd",
	StatV2: "stat_v2",
	ListV2: "ls_v2",
	FixedPushMkdir: "fixed_push_mkdir",
	Abb: "abb",
	AbbExec: "abb_exec",
	SendReceiveV2: "sendrecv_v2",
	DelayedAck: "delayed_ack"
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/shell/shared.js
var AdbShellProtocolId = {
	Stdin: 0,
	Stdout: 1,
	Stderr: 2,
	Exit: 3,
	CloseStdin: 4,
	WindowSizeChange: 5
};
var AdbShellProtocolPacket = /* @__PURE__ */ struct({
	id: u8(),
	data: /* @__PURE__ */ buffer(u32)
}, { littleEndian: true });
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/shell/process.js
var AdbShellProtocolProcessImpl = class {
	#socket;
	#writer;
	#stdin;
	get stdin() {
		return this.#stdin;
	}
	#stdout;
	get stdout() {
		return this.#stdout;
	}
	#stderr;
	get stderr() {
		return this.#stderr;
	}
	#exited;
	get exited() {
		return this.#exited;
	}
	constructor(socket, signal) {
		this.#socket = socket;
		let stdoutController;
		let stderrController;
		this.#stdout = new PushReadableStream((controller) => {
			stdoutController = controller;
		});
		this.#stderr = new PushReadableStream((controller) => {
			stderrController = controller;
		});
		const exited = new PromiseResolver();
		this.#exited = exited.promise;
		socket.readable.pipeThrough(new StructDeserializeStream(AdbShellProtocolPacket)).pipeTo(new WritableStream({ write: async (chunk) => {
			switch (chunk.id) {
				case AdbShellProtocolId.Exit:
					exited.resolve(chunk.data[0]);
					break;
				case AdbShellProtocolId.Stdout:
					await stdoutController.enqueue(chunk.data);
					break;
				case AdbShellProtocolId.Stderr:
					await stderrController.enqueue(chunk.data);
					break;
				default: break;
			}
		} })).then(() => {
			stdoutController.close();
			stderrController.close();
			exited.reject(/* @__PURE__ */ new Error("Socket ended without exit message"));
		}, (e) => {
			stdoutController.error(e);
			stderrController.error(e);
			exited.reject(e);
		});
		if (signal) signal.addEventListener("abort", () => {
			exited.reject(signal.reason);
			this.#socket.close();
		});
		this.#writer = this.#socket.writable.getWriter();
		this.#stdin = new MaybeConsumableWritableStream({
			write: async (chunk) => {
				await this.#writer.write(AdbShellProtocolPacket.serialize({
					id: AdbShellProtocolId.Stdin,
					data: chunk
				}));
			},
			close: () => this.#writer.write(AdbShellProtocolPacket.serialize({
				id: AdbShellProtocolId.CloseStdin,
				data: EmptyUint8Array
			}))
		});
	}
	kill() {
		return this.#socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/shell/pty.js
var AdbShellProtocolPtyProcess = class {
	#socket;
	#writer;
	#input;
	get input() {
		return this.#input;
	}
	#stdout;
	get output() {
		return this.#stdout;
	}
	#exited = new PromiseResolver();
	get exited() {
		return this.#exited.promise;
	}
	constructor(socket) {
		this.#socket = socket;
		let stdoutController;
		this.#stdout = new PushReadableStream((controller) => {
			stdoutController = controller;
		});
		socket.readable.pipeThrough(new StructDeserializeStream(AdbShellProtocolPacket)).pipeTo(new WritableStream({ write: async (chunk) => {
			switch (chunk.id) {
				case AdbShellProtocolId.Exit:
					this.#exited.resolve(chunk.data[0]);
					break;
				case AdbShellProtocolId.Stdout:
					await stdoutController.enqueue(chunk.data);
					break;
			}
		} })).then(() => {
			stdoutController.close();
			this.#exited.reject(/* @__PURE__ */ new Error("Socket ended without exit message"));
		}, (e) => {
			stdoutController.error(e);
			this.#exited.reject(e);
		});
		this.#writer = this.#socket.writable.getWriter();
		this.#input = new MaybeConsumableWritableStream({ write: (chunk) => this.#writeStdin(chunk) });
	}
	#writeStdin(chunk) {
		return this.#writer.write(AdbShellProtocolPacket.serialize({
			id: AdbShellProtocolId.Stdin,
			data: chunk
		}));
	}
	async resize(rows, cols) {
		await this.#writer.write(AdbShellProtocolPacket.serialize({
			id: AdbShellProtocolId.WindowSizeChange,
			data: /* @__PURE__ */ encodeUtf8(`${rows}x${cols},0x0\0`)
		}));
	}
	sigint() {
		return this.#writeStdin(new Uint8Array([3]));
	}
	kill() {
		return this.#socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/shell/spawner.js
var AdbShellProtocolSpawner = class {
	#spawn;
	constructor(spawn) {
		this.#spawn = spawn;
	}
	spawn(command, signal) {
		signal?.throwIfAborted();
		if (typeof command === "string") command = splitCommand(command);
		return this.#spawn(command, signal);
	}
	async spawnWait(command) {
		const process = await this.spawn(command);
		const [stdout, stderr, exitCode] = await Promise.all([
			process.stdout.pipeThrough(new ConcatBufferStream()),
			process.stderr.pipeThrough(new ConcatBufferStream()),
			process.exited
		]);
		return {
			stdout,
			stderr,
			exitCode
		};
	}
	async spawnWaitText(command) {
		const process = await this.spawn(command);
		const [stdout, stderr, exitCode] = await Promise.all([
			process.stdout.pipeThrough(new TextDecoderStream()).pipeThrough(new ConcatStringStream()),
			process.stderr.pipeThrough(new TextDecoderStream()).pipeThrough(new ConcatStringStream()),
			process.exited
		]);
		return {
			stdout,
			stderr,
			exitCode
		};
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/shell/service.js
var AdbShellProtocolSubprocessService = class extends AdbShellProtocolSpawner {
	#adb;
	get adb() {
		return this.#adb;
	}
	get isSupported() {
		return this.#adb.canUseFeature(AdbFeature.ShellV2);
	}
	constructor(adb) {
		super(async (command, signal) => {
			const socket = await this.#adb.createSocket(`shell,v2,raw:${command.join(" ")}`);
			if (signal?.aborted) {
				await socket.close();
				throw signal.reason;
			}
			return new AdbShellProtocolProcessImpl(socket, signal);
		});
		this.#adb = adb;
	}
	async pty(options) {
		let service = "shell,v2,pty";
		if (options?.terminalType) service += `,TERM=` + options.terminalType;
		service += ":";
		if (options) {
			if (typeof options.command === "string") service += options.command;
			else if (Array.isArray(options.command)) service += options.command.join(" ");
		}
		return new AdbShellProtocolPtyProcess(await this.#adb.createSocket(service));
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/subprocess/service.js
var AdbSubprocessService = class {
	#adb;
	get adb() {
		return this.#adb;
	}
	#noneProtocol;
	get noneProtocol() {
		return this.#noneProtocol;
	}
	#shellProtocol;
	get shellProtocol() {
		return this.#shellProtocol;
	}
	constructor(adb) {
		this.#adb = adb;
		this.#noneProtocol = new AdbNoneProtocolSubprocessService(adb);
		if (adb.canUseFeature(AdbFeature.ShellV2)) this.#shellProtocol = new AdbShellProtocolSubprocessService(adb);
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/response.js
function encodeAsciiUnchecked(value) {
	const result = new Uint8Array(value.length);
	for (let i = 0; i < value.length; i += 1) result[i] = value.charCodeAt(i);
	return result;
}
/**
* Encode ID to numbers for faster comparison
* @param value A 4-character string
* @returns A 32-bit integer by encoding the string as little-endian
*
* #__NO_SIDE_EFFECTS__
*/
function adbSyncEncodeId(value) {
	return /* @__PURE__ */ getUint32LittleEndian(encodeAsciiUnchecked(value), 0);
}
var AdbSyncResponseId = {
	Entry: adbSyncEncodeId("DENT"),
	Entry2: adbSyncEncodeId("DNT2"),
	Lstat: adbSyncEncodeId("STAT"),
	Stat: adbSyncEncodeId("STA2"),
	Lstat2: adbSyncEncodeId("LST2"),
	Done: adbSyncEncodeId("DONE"),
	Data: adbSyncEncodeId("DATA"),
	Ok: adbSyncEncodeId("OKAY"),
	Fail: adbSyncEncodeId("FAIL")
};
var AdbSyncError = class extends Error {};
var AdbSyncFailResponse = /* @__PURE__ */ struct({ message: /* @__PURE__ */ string(u32) }, {
	littleEndian: true,
	postDeserialize(value) {
		throw new AdbSyncError(value.message);
	}
});
async function adbSyncReadResponse(stream, id, type) {
	if (typeof id === "string") id = adbSyncEncodeId(id);
	const buffer = await stream.readExactly(4);
	switch (/* @__PURE__ */ getUint32LittleEndian(buffer, 0)) {
		case AdbSyncResponseId.Fail:
			await AdbSyncFailResponse.deserialize(stream);
			throw new Error("Unreachable");
		case id: return await type.deserialize(stream);
		default: throw new Error(`Expected '${id}', but got '${/* @__PURE__ */ decodeUtf8(buffer)}'`);
	}
}
async function* adbSyncReadResponses(stream, id, type) {
	if (typeof id === "string") id = adbSyncEncodeId(id);
	while (true) {
		const buffer = await stream.readExactly(4);
		switch (/* @__PURE__ */ getUint32LittleEndian(buffer, 0)) {
			case AdbSyncResponseId.Fail:
				await AdbSyncFailResponse.deserialize(stream);
				unreachable();
			case AdbSyncResponseId.Done:
				await stream.readExactly(type.size);
				return;
			case id:
				yield await type.deserialize(stream);
				break;
			default: throw new Error(`Expected '${id}' or '${AdbSyncResponseId.Done}', but got '${/* @__PURE__ */ decodeUtf8(buffer)}'`);
		}
	}
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/request.js
var AdbSyncRequestId = {
	List: adbSyncEncodeId("LIST"),
	ListV2: adbSyncEncodeId("LIS2"),
	Send: adbSyncEncodeId("SEND"),
	SendV2: adbSyncEncodeId("SND2"),
	Lstat: adbSyncEncodeId("STAT"),
	Stat: adbSyncEncodeId("STA2"),
	LstatV2: adbSyncEncodeId("LST2"),
	Data: adbSyncEncodeId("DATA"),
	Done: adbSyncEncodeId("DONE"),
	Receive: adbSyncEncodeId("RECV")
};
var AdbSyncNumberRequest = /* @__PURE__ */ struct({
	id: u32,
	arg: u32
}, { littleEndian: true });
async function adbSyncWriteRequest(writable, id, value) {
	if (typeof id === "string") id = adbSyncEncodeId(id);
	if (typeof value === "number") {
		await writable.write(AdbSyncNumberRequest.serialize({
			id,
			arg: value
		}));
		return;
	}
	if (typeof value === "string") value = /* @__PURE__ */ encodeUtf8(value);
	await writable.write(AdbSyncNumberRequest.serialize({
		id,
		arg: value.length
	}));
	await writable.write(value);
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/stat.js
var LinuxFileType = {
	Directory: 4,
	File: 8,
	Link: 10
};
var AdbSyncLstatResponse = /* @__PURE__ */ struct({
	mode: u32,
	size: u32,
	mtime: u32
}, {
	littleEndian: true,
	extra: {
		get type() {
			return this.mode >> 12;
		},
		get permission() {
			return this.mode & 4095;
		}
	},
	postDeserialize(value) {
		if (value.mode === 0 && value.size === 0 && value.mtime === 0) throw new Error("lstat error");
		return value;
	}
});
var AdbSyncStatErrorCode = {
	SUCCESS: 0,
	EACCES: 13,
	EEXIST: 17,
	EFAULT: 14,
	EFBIG: 27,
	EINTR: 4,
	EINVAL: 22,
	EIO: 5,
	EISDIR: 21,
	ELOOP: 40,
	EMFILE: 24,
	ENAMETOOLONG: 36,
	ENFILE: 23,
	ENOENT: 2,
	ENOMEM: 12,
	ENOSPC: 28,
	ENOTDIR: 20,
	EOVERFLOW: 75,
	EPERM: 1,
	EROFS: 30,
	ETXTBSY: 26
};
var AdbSyncStatErrorName = /* @__PURE__ */ (() => Object.fromEntries(Object.entries(AdbSyncStatErrorCode).map(([key, value]) => [value, key])))();
var AdbSyncStatResponse = /* @__PURE__ */ struct({
	error: u32(),
	dev: u64,
	ino: u64,
	mode: u32,
	nlink: u32,
	uid: u32,
	gid: u32,
	size: u64,
	atime: u64,
	mtime: u64,
	ctime: u64
}, {
	littleEndian: true,
	extra: {
		get type() {
			return this.mode >> 12;
		},
		get permission() {
			return this.mode & 4095;
		}
	},
	postDeserialize(value) {
		if (value.error) throw new Error(AdbSyncStatErrorName[value.error]);
		return value;
	}
});
async function adbSyncLstat(socket, path, v2) {
	const locked = await socket.lock();
	try {
		if (v2) {
			await adbSyncWriteRequest(locked, AdbSyncRequestId.LstatV2, path);
			return await adbSyncReadResponse(locked, AdbSyncResponseId.Lstat2, AdbSyncStatResponse);
		} else {
			await adbSyncWriteRequest(locked, AdbSyncRequestId.Lstat, path);
			const response = await adbSyncReadResponse(locked, AdbSyncResponseId.Lstat, AdbSyncLstatResponse);
			return {
				mode: response.mode,
				size: BigInt(response.size),
				mtime: BigInt(response.mtime),
				get type() {
					return response.type;
				},
				get permission() {
					return response.permission;
				}
			};
		}
	} finally {
		locked.release();
	}
}
async function adbSyncStat(socket, path) {
	const locked = await socket.lock();
	try {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.Stat, path);
		return await adbSyncReadResponse(locked, AdbSyncResponseId.Stat, AdbSyncStatResponse);
	} finally {
		locked.release();
	}
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/list.js
var AdbSyncEntryResponse = /* @__PURE__ */ extend(AdbSyncLstatResponse, { name: /* @__PURE__ */ string(u32) });
var AdbSyncEntry2Response = /* @__PURE__ */ extend(AdbSyncStatResponse, { name: /* @__PURE__ */ string(u32) });
async function* adbSyncOpenDirV2(socket, path) {
	const locked = await socket.lock();
	try {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.ListV2, path);
		for await (const item of adbSyncReadResponses(locked, AdbSyncResponseId.Entry2, AdbSyncEntry2Response)) {
			if (item.error !== AdbSyncStatErrorCode.SUCCESS) continue;
			yield item;
		}
	} finally {
		locked.release();
	}
}
async function* adbSyncOpenDirV1(socket, path) {
	const locked = await socket.lock();
	try {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.List, path);
		for await (const item of adbSyncReadResponses(locked, AdbSyncResponseId.Entry, AdbSyncEntryResponse)) yield item;
	} finally {
		locked.release();
	}
}
async function* adbSyncOpenDir(socket, path, v2) {
	if (v2) yield* adbSyncOpenDirV2(socket, path);
	else for await (const item of adbSyncOpenDirV1(socket, path)) yield {
		mode: item.mode,
		size: BigInt(item.size),
		mtime: BigInt(item.mtime),
		get type() {
			return item.type;
		},
		get permission() {
			return item.permission;
		},
		name: item.name
	};
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/pull.js
var AdbSyncDataResponse = /* @__PURE__ */ struct({ data: /* @__PURE__ */ buffer(u32) }, { littleEndian: true });
async function* adbSyncPullGenerator(socket, path) {
	const locked = await socket.lock();
	let done = false;
	try {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.Receive, path);
		for await (const packet of adbSyncReadResponses(locked, AdbSyncResponseId.Data, AdbSyncDataResponse)) yield packet.data;
		done = true;
	} catch (e) {
		done = true;
		throw e;
	} finally {
		if (!done) for await (const packet of adbSyncReadResponses(locked, AdbSyncResponseId.Data, AdbSyncDataResponse));
		locked.release();
	}
}
function adbSyncPull(socket, path) {
	return ReadableStream.from(adbSyncPullGenerator(socket, path));
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/push.js
var ADB_SYNC_MAX_PACKET_SIZE = 64 * 1024;
var AdbSyncOkResponse = /* @__PURE__ */ struct({ unused: u32 }, { littleEndian: true });
async function pipeFileData(locked, file, packetSize, mtime) {
	const abortController = new AbortController();
	file.pipeThrough(new DistributionStream(packetSize, true)).pipeTo(new MaybeConsumableWritableStream({ write(chunk) {
		return adbSyncWriteRequest(locked, AdbSyncRequestId.Data, chunk);
	} }), { signal: abortController.signal }).then(async () => {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.Done, mtime);
		await locked.flush();
	}, NOOP);
	await adbSyncReadResponse(locked, AdbSyncResponseId.Ok, AdbSyncOkResponse).catch((e) => {
		abortController.abort();
		throw e;
	});
}
async function adbSyncPushV1({ socket, filename, file, type = LinuxFileType.File, permission = 438, mtime = Date.now() / 1e3 | 0, packetSize = ADB_SYNC_MAX_PACKET_SIZE }) {
	const locked = await socket.lock();
	try {
		const pathAndMode = `${filename},${(type << 12 | permission).toString()}`;
		await adbSyncWriteRequest(locked, AdbSyncRequestId.Send, pathAndMode);
		await pipeFileData(locked, file, packetSize, mtime);
	} finally {
		locked.release();
	}
}
var AdbSyncSendV2Flags = {
	None: 0,
	Brotli: 1,
	/**
	* 2
	*/
	Lz4: 2,
	/**
	* 4
	*/
	Zstd: 4,
	DryRun: 2147483648
};
var AdbSyncSendV2Request = /* @__PURE__ */ struct({
	id: u32,
	mode: u32,
	flags: u32()
}, { littleEndian: true });
async function adbSyncPushV2({ socket, filename, file, type = LinuxFileType.File, permission = 438, mtime = Date.now() / 1e3 | 0, packetSize = ADB_SYNC_MAX_PACKET_SIZE, dryRun = false }) {
	const locked = await socket.lock();
	try {
		await adbSyncWriteRequest(locked, AdbSyncRequestId.SendV2, filename);
		const mode = type << 12 | permission;
		let flags = AdbSyncSendV2Flags.None;
		if (dryRun) flags |= AdbSyncSendV2Flags.DryRun;
		await locked.write(AdbSyncSendV2Request.serialize({
			id: AdbSyncRequestId.SendV2,
			mode,
			flags
		}));
		await pipeFileData(locked, file, packetSize, mtime);
	} finally {
		locked.release();
	}
}
function adbSyncPush(options) {
	if (options.v2) return adbSyncPushV2(options);
	if (options.dryRun) throw new Error("dryRun is not supported in v1");
	return adbSyncPushV1(options);
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/socket.js
var AdbSyncSocketLocked = class {
	#writer;
	#readable;
	#socketLock;
	#writeLock = new AutoResetEvent();
	#combiner;
	get position() {
		return this.#readable.position;
	}
	constructor(writer, readable, bufferSize, lock) {
		this.#writer = writer;
		this.#readable = readable;
		this.#socketLock = lock;
		this.#combiner = new BufferCombiner(bufferSize);
	}
	#write(buffer) {
		return Consumable.WritableStream.write(this.#writer, buffer);
	}
	async flush() {
		try {
			await this.#writeLock.wait();
			const buffer = this.#combiner.flush();
			if (buffer) await this.#write(buffer);
		} finally {
			this.#writeLock.notifyOne();
		}
	}
	async write(data) {
		try {
			await this.#writeLock.wait();
			for (const buffer of this.#combiner.push(data)) await this.#write(buffer);
		} finally {
			this.#writeLock.notifyOne();
		}
	}
	async readExactly(length) {
		await this.flush();
		return await this.#readable.readExactly(length);
	}
	release() {
		this.#combiner.flush();
		this.#socketLock.notifyOne();
	}
	async close() {
		await this.#readable.cancel();
	}
};
var AdbSyncSocket = class {
	#lock = new AutoResetEvent();
	#socket;
	#locked;
	constructor(socket, bufferSize) {
		this.#socket = socket;
		this.#locked = new AdbSyncSocketLocked(socket.writable.getWriter(), new BufferedReadableStream(socket.readable), bufferSize, this.#lock);
	}
	async lock() {
		await this.#lock.wait();
		return this.#locked;
	}
	async close() {
		await this.#locked.close();
		await this.#socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/sync/sync.js
/**
* A simplified `dirname` function that only handles absolute unix paths.
* @param path an absolute unix path
* @returns the directory name of the input path
*/
function dirname(path) {
	const end = path.lastIndexOf("/");
	if (end === -1) throw new Error(`Invalid path`);
	if (end === 0) return "/";
	return path.substring(0, end);
}
var AdbSync = class {
	_adb;
	_socket;
	#supportsStat;
	#supportsListV2;
	#fixedPushMkdir;
	#supportsSendReceiveV2;
	#needPushMkdirWorkaround;
	get supportsStat() {
		return this.#supportsStat;
	}
	get supportsListV2() {
		return this.#supportsListV2;
	}
	get fixedPushMkdir() {
		return this.#fixedPushMkdir;
	}
	get supportsSendReceiveV2() {
		return this.#supportsSendReceiveV2;
	}
	get needPushMkdirWorkaround() {
		return this.#needPushMkdirWorkaround;
	}
	constructor(adb, socket) {
		this._adb = adb;
		this._socket = new AdbSyncSocket(socket, adb.maxPayloadSize);
		this.#supportsStat = adb.canUseFeature(AdbFeature.StatV2);
		this.#supportsListV2 = adb.canUseFeature(AdbFeature.ListV2);
		this.#fixedPushMkdir = adb.canUseFeature(AdbFeature.FixedPushMkdir);
		this.#supportsSendReceiveV2 = adb.canUseFeature(AdbFeature.SendReceiveV2);
		this.#needPushMkdirWorkaround = this._adb.canUseFeature(AdbFeature.ShellV2) && !this.fixedPushMkdir;
	}
	/**
	* Gets information of a file or folder.
	*
	* If `path` points to a symbolic link, the returned information is about the link itself (with `type` being `LinuxFileType.Link`).
	*/
	async lstat(path) {
		return await adbSyncLstat(this._socket, path, this.#supportsStat);
	}
	/**
	* Gets the information of a file or folder.
	*
	* If `path` points to a symbolic link, it will be resolved and the returned information is about the target (with `type` being `LinuxFileType.File` or `LinuxFileType.Directory`).
	*/
	async stat(path) {
		if (!this.#supportsStat) throw new Error("Not supported");
		return await adbSyncStat(this._socket, path);
	}
	/**
	* Checks if `path` is a directory, or a symbolic link to a directory.
	*
	* This uses `lstat` internally, thus works on all Android versions.
	*/
	async isDirectory(path) {
		try {
			await this.lstat(path + "/");
			return true;
		} catch {
			return false;
		}
	}
	opendir(path) {
		return adbSyncOpenDir(this._socket, path, this.supportsListV2);
	}
	async readdir(path) {
		const results = [];
		for await (const entry of this.opendir(path)) results.push(entry);
		return results;
	}
	/**
	* Reads the content of a file on device.
	*
	* @param filename The full path of the file on device to read.
	* @returns A `ReadableStream` that contains the file content.
	*/
	read(filename) {
		return adbSyncPull(this._socket, filename);
	}
	/**
	* Writes a file on device. If the file name already exists, it will be overwritten.
	*
	* @param options The content and options of the file to write.
	*/
	async write(options) {
		if (this.needPushMkdirWorkaround) await this._adb.subprocess.noneProtocol.spawnWait([
			"mkdir",
			"-p",
			escapeArg(dirname(options.filename))
		]);
		await adbSyncPush({
			v2: this.supportsSendReceiveV2,
			socket: this._socket,
			...options
		});
	}
	lockSocket() {
		return this._socket.lock();
	}
	dispose() {
		return this._socket.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/commands/tcpip.js
function parsePort(value) {
	if (!value || value === "0") return;
	return Number.parseInt(value, 10);
}
var AdbTcpIpService = class extends AdbServiceBase {
	async getListenAddresses() {
		const serviceListenAddresses = await this.adb.getProp("service.adb.listen_addrs");
		const servicePort = await this.adb.getProp("service.adb.tcp.port");
		const persistPort = await this.adb.getProp("persist.adb.tcp.port");
		return {
			serviceListenAddresses: serviceListenAddresses != "" ? serviceListenAddresses.split(",") : [],
			servicePort: parsePort(servicePort),
			persistPort: parsePort(persistPort)
		};
	}
	async setPort(port) {
		if (port <= 0) throw new TypeError(`Invalid port ${port}`);
		const output = await this.adb.createSocketAndWait(`tcpip:${port}`);
		if (output !== `restarting in TCP mode port: ${port}\n`) throw new Error(output);
		return output;
	}
	async disable() {
		const output = await this.adb.createSocketAndWait("usb:");
		if (output !== "restarting in USB mode\n") throw new Error(output);
		return output;
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/adb.js
var Adb = class {
	#transport;
	get transport() {
		return this.#transport;
	}
	get serial() {
		return this.#transport.serial;
	}
	get maxPayloadSize() {
		return this.#transport.maxPayloadSize;
	}
	get banner() {
		return this.#transport.banner;
	}
	get disconnected() {
		return this.#transport.disconnected;
	}
	get clientFeatures() {
		return this.#transport.clientFeatures;
	}
	get deviceFeatures() {
		return this.banner.features;
	}
	subprocess;
	power;
	reverse;
	tcpip;
	constructor(transport) {
		this.#transport = transport;
		this.subprocess = new AdbSubprocessService(this);
		this.power = new AdbPower(this);
		this.reverse = new AdbReverseService(this);
		this.tcpip = new AdbTcpIpService(this);
	}
	canUseFeature(feature) {
		return this.clientFeatures.includes(feature) && this.deviceFeatures.includes(feature);
	}
	/**
	* Creates a new ADB Socket to the specified service or socket address.
	*/
	async createSocket(service) {
		return this.#transport.connect(service);
	}
	async createSocketAndWait(service) {
		return await (await this.createSocket(service)).readable.pipeThrough(new TextDecoderStream()).pipeThrough(new ConcatStringStream());
	}
	getProp(key) {
		return this.subprocess.noneProtocol.spawnWaitText(["getprop", key]).then((output) => output.trim());
	}
	rm(filenames, options) {
		const args = ["rm"];
		if (options?.recursive) args.push("-r");
		if (options?.force) args.push("-f");
		if (Array.isArray(filenames)) for (const filename of filenames) args.push(escapeArg(filename));
		else args.push(escapeArg(filenames));
		args.push("</dev/null");
		return this.subprocess.noneProtocol.spawnWaitText(args);
	}
	async sync() {
		const socket = await this.createSocket("sync:");
		return new AdbSync(this, socket);
	}
	async framebuffer() {
		return framebuffer(this);
	}
	async close() {
		await this.#transport.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/banner.js
var AdbBannerKey = {
	Product: "ro.product.name",
	Model: "ro.product.model",
	Device: "ro.product.device",
	Features: "features"
};
var AdbBanner = class AdbBanner {
	static parse(banner) {
		let state;
		let product;
		let model;
		let device;
		let features = [];
		const pieces = banner.split("::");
		if (pieces.length > 1) {
			state = pieces[0].trim() || void 0;
			const props = pieces[1];
			for (const prop of props.split(";")) {
				// istanbul ignore if
				if (!prop) continue;
				const keyValue = prop.split("=");
				if (keyValue.length !== 2) continue;
				const [key, value] = keyValue;
				switch (key) {
					case AdbBannerKey.Product:
						product = value;
						break;
					case AdbBannerKey.Model:
						model = value;
						break;
					case AdbBannerKey.Device:
						device = value;
						break;
					case AdbBannerKey.Features:
						features = value.split(",");
						break;
				}
			}
		}
		return new AdbBanner(state, product, model, device, features);
	}
	#state;
	get state() {
		return this.#state;
	}
	#product;
	get product() {
		return this.#product;
	}
	#model;
	get model() {
		return this.#model;
	}
	#device;
	get device() {
		return this.#device;
	}
	#features = [];
	get features() {
		return this.#features;
	}
	constructor(state, product, model, device, features) {
		this.#state = state;
		this.#product = product;
		this.#model = model;
		this.#device = device;
		this.#features = features;
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/crypto.js
/**
* Gets the `BigInt` value at the specified byte offset and length from the start of the view. There is
* no alignment constraint; multi-byte values may be fetched from any offset.
*
* Only supports Big-Endian, because that's what ADB uses.
* @param byteOffset The place in the buffer at which the value should be retrieved.
*/
function getBigUint(array, byteOffset, length) {
	let result = 0n;
	for (let i = byteOffset; i < byteOffset + length; i += 8) {
		result <<= 64n;
		const value = getUint64BigEndian(array, i);
		result |= value;
	}
	return result;
}
/**
* Stores an arbitrary-precision positive `BigInt` value at the specified byte offset from the start of the view.
* @param byteOffset The place in the buffer at which the value should be set.
* @param length The number of bytes to set.
* @param value The value to set.
* @param littleEndian If `false` or `undefined`, a big-endian value should be written,
* otherwise a little-endian value should be written.
*/
function setBigUint(array, byteOffset, length, value, littleEndian) {
	if (littleEndian) while (value > 0n) {
		setInt64LittleEndian(array, byteOffset, value);
		byteOffset += 8;
		value >>= 64n;
	}
	else {
		let position = byteOffset + length - 8;
		while (value > 0n) {
			setInt64BigEndian(array, position, value);
			position -= 8;
			value >>= 64n;
		}
	}
}
var RsaPrivateKeyNOffset = 38;
var RsaPrivateKeyNLength = 2048 / 8;
var RsaPrivateKeyDOffset = 303;
var RsaPrivateKeyDLength = 2048 / 8;
function rsaParsePrivateKey(key) {
	return [getBigUint(key, RsaPrivateKeyNOffset, RsaPrivateKeyNLength), getBigUint(key, RsaPrivateKeyDOffset, RsaPrivateKeyDLength)];
}
function nonNegativeMod(m, d) {
	const r = m % d;
	if (r > 0) return r;
	return r + (d > 0 ? d : -d);
}
function modInverse(a, m) {
	a = nonNegativeMod(a, m);
	if (!a || m < 2) return NaN;
	const s = [];
	let b = m;
	while (b) {
		[a, b] = [b, a % b];
		s.push({
			a,
			b
		});
	}
	/* istanbul ignore next */
	if (a !== 1) return NaN;
	let x = 1;
	let y = 0;
	for (let i = s.length - 2; i >= 0; i -= 1) [x, y] = [y, x - y * Math.floor(s[i].a / s[i].b)];
	return nonNegativeMod(y, m);
}
var ModulusLengthInBytes = 2048 / 8;
var ModulusLengthInWords = ModulusLengthInBytes / 4;
function adbGetPublicKeySize() {
	return 524;
}
function adbGeneratePublicKey(privateKey, output) {
	let outputType;
	const outputLength = adbGetPublicKeySize();
	if (!output) {
		output = new Uint8Array(outputLength);
		outputType = "Uint8Array";
	} else {
		if (output.length < outputLength) throw new TypeError("output buffer is too small");
		outputType = "number";
	}
	const outputView = new DataView(output.buffer, output.byteOffset, output.length);
	let outputOffset = 0;
	outputView.setUint32(outputOffset, ModulusLengthInWords, true);
	outputOffset += 4;
	const [n] = rsaParsePrivateKey(privateKey);
	const n0inv = -modInverse(Number(n % 2n ** 32n), 2 ** 32);
	outputView.setInt32(outputOffset, n0inv, true);
	outputOffset += 4;
	setBigUint(output, outputOffset, ModulusLengthInBytes, n, true);
	outputOffset += ModulusLengthInBytes;
	const rr = 2n ** 4096n % n;
	setBigUint(output, outputOffset, ModulusLengthInBytes, rr, true);
	outputOffset += ModulusLengthInBytes;
	outputView.setUint32(outputOffset, 65537, true);
	outputOffset += 4;
	if (outputType === "Uint8Array") return output;
	else return outputLength;
}
/**
* Modular exponentiation.
*
* Calculate `(base ** exponent) % modulus` without actually calculating `(base ** exponent)`.
*
* See https://en.wikipedia.org/wiki/Modular_exponentiation#Implementation_in_Lua
*/
function powMod(base, exponent, modulus) {
	if (modulus === 1n) return 0n;
	let r = 1n;
	base = base % modulus;
	while (exponent > 0n) {
		if (BigInt.asUintN(1, exponent) === 1n) r = r * base % modulus;
		base = base * base % modulus;
		exponent >>= 1n;
	}
	return r;
}
var SHA1_DIGEST_LENGTH = 20;
var ASN1_SEQUENCE = 48;
var ASN1_OCTET_STRING = 4;
var ASN1_NULL = 5;
var ASN1_OID = 6;
var SHA1_DIGEST_INFO = new Uint8Array([
	48,
	33,
	48,
	9,
	6,
	5,
	43,
	14,
	3,
	2,
	26,
	5,
	0,
	4,
	20
]);
function rsaSign(privateKey, data) {
	const [n, d] = rsaParsePrivateKey(privateKey);
	const padded = new Uint8Array(256);
	let index = 0;
	padded[index] = 0;
	index += 1;
	padded[index] = 1;
	index += 1;
	const fillLength = padded.length - SHA1_DIGEST_INFO.length - data.length - 1;
	while (index < fillLength) {
		padded[index] = 255;
		index += 1;
	}
	padded[index] = 0;
	index += 1;
	padded.set(SHA1_DIGEST_INFO, index);
	index += SHA1_DIGEST_INFO.length;
	padded.set(data, index);
	const signature = powMod(getBigUint(padded, 0, padded.length), d, n);
	setBigUint(padded, 0, padded.length, signature, false);
	return padded;
}
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/packet.js
var AdbCommand = {
	Auth: 1213486401,
	Close: 1163086915,
	Connect: 1314410051,
	Okay: 1497451343,
	Open: 1313165391,
	Write: 1163154007
};
var AdbPacketHeader = /* @__PURE__ */ struct({
	command: u32,
	arg0: u32,
	arg1: u32,
	payloadLength: u32,
	checksum: u32,
	magic: s32
}, { littleEndian: true });
var AdbPacket = /* @__PURE__ */ extend(AdbPacketHeader, { payload: /* @__PURE__ */ buffer("payloadLength") });
function calculateChecksum(payload) {
	return payload.reduce((result, item) => result + item, 0);
}
var AdbPacketSerializeStream = class extends TransformStream {
	constructor() {
		const headerBuffer = new Uint8Array(AdbPacketHeader.size);
		super({ transform: async (chunk, controller) => {
			await chunk.tryConsume(async (chunk) => {
				const init = chunk;
				init.payloadLength = init.payload.length;
				AdbPacketHeader.serialize(init, headerBuffer);
				await Consumable.ReadableStream.enqueue(controller, headerBuffer);
				if (init.payloadLength) await Consumable.ReadableStream.enqueue(controller, init.payload);
			});
		} });
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/auth.js
var AdbAuthType = {
	Token: 1,
	Signature: 2,
	PublicKey: 3
};
var AdbSignatureAuthenticator = async function* (credentialStore, getNextRequest) {
	for await (const key of credentialStore.iterateKeys()) {
		const packet = await getNextRequest();
		if (packet.arg0 !== AdbAuthType.Token) return;
		const signature = rsaSign(key.buffer, packet.payload);
		yield {
			command: AdbCommand.Auth,
			arg0: AdbAuthType.Signature,
			arg1: 0,
			payload: signature
		};
	}
};
var AdbPublicKeyAuthenticator = async function* (credentialStore, getNextRequest) {
	if ((await getNextRequest()).arg0 !== AdbAuthType.Token) return;
	let privateKey;
	for await (const key of credentialStore.iterateKeys()) {
		privateKey = key;
		break;
	}
	if (!privateKey) privateKey = await credentialStore.generateKey();
	const publicKeyLength = adbGetPublicKeySize();
	const [publicKeyBase64Length] = calculateBase64EncodedLength(publicKeyLength);
	const nameBuffer = privateKey.name?.length ? /* @__PURE__ */ encodeUtf8(privateKey.name) : EmptyUint8Array;
	const publicKeyBuffer = new Uint8Array(publicKeyBase64Length + (nameBuffer.length ? nameBuffer.length + 1 : 0) + 1);
	adbGeneratePublicKey(privateKey.buffer, publicKeyBuffer);
	encodeBase64(publicKeyBuffer.subarray(0, publicKeyLength), publicKeyBuffer);
	if (nameBuffer.length) {
		publicKeyBuffer[publicKeyBase64Length] = 32;
		publicKeyBuffer.set(nameBuffer, publicKeyBase64Length + 1);
	}
	yield {
		command: AdbCommand.Auth,
		arg0: AdbAuthType.PublicKey,
		arg1: 0,
		payload: publicKeyBuffer
	};
};
var ADB_DEFAULT_AUTHENTICATORS = [AdbSignatureAuthenticator, AdbPublicKeyAuthenticator];
var AdbAuthenticationProcessor = class {
	authenticators;
	#credentialStore;
	#pendingRequest = new PromiseResolver();
	#iterator;
	constructor(authenticators, credentialStore) {
		this.authenticators = authenticators;
		this.#credentialStore = credentialStore;
	}
	#getNextRequest = () => {
		return this.#pendingRequest.promise;
	};
	async *#invokeAuthenticator() {
		for (const authenticator of this.authenticators) for await (const packet of authenticator(this.#credentialStore, this.#getNextRequest)) {
			this.#pendingRequest = new PromiseResolver();
			yield packet;
		}
	}
	async process(packet) {
		if (!this.#iterator) this.#iterator = this.#invokeAuthenticator();
		this.#pendingRequest.resolve(packet);
		const result = await this.#iterator.next();
		if (result.done) throw new Error("No authenticator can handle the request");
		return result.value;
	}
	dispose() {
		this.#iterator?.return?.();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/socket.js
var AdbDaemonSocketController = class {
	#dispatcher;
	localId;
	remoteId;
	localCreated;
	service;
	#readable;
	#readableController;
	get readable() {
		return this.#readable;
	}
	#writableController;
	writable;
	#closed = false;
	#closedPromise = new PromiseResolver();
	get closed() {
		return this.#closedPromise.promise;
	}
	#socket;
	get socket() {
		return this.#socket;
	}
	#availableWriteBytesChanged;
	/**
	* When delayed ack is disabled, returns `Infinity` if the socket is ready to write
	* (exactly one packet can be written no matter how large it is), or `-1` if the socket
	* is waiting for ack message.
	*
	* When delayed ack is enabled, returns a non-negative finite number indicates the number of
	* bytes that can be written to the socket before waiting for ack message.
	*/
	#availableWriteBytes = 0;
	constructor(options) {
		this.#dispatcher = options.dispatcher;
		this.localId = options.localId;
		this.remoteId = options.remoteId;
		this.localCreated = options.localCreated;
		this.service = options.service;
		this.#readable = new PushReadableStream((controller) => {
			this.#readableController = controller;
		});
		this.writable = new MaybeConsumableWritableStream({
			start: (controller) => {
				this.#writableController = controller;
				controller.signal.addEventListener("abort", () => {
					this.#availableWriteBytesChanged?.reject(controller.signal.reason);
				});
			},
			write: async (data) => {
				const size = data.length;
				const chunkSize = this.#dispatcher.options.maxPayloadSize;
				for (let start = 0, end = chunkSize; start < size; start = end, end += chunkSize) {
					const chunk = data.subarray(start, end);
					await this.#writeChunk(chunk);
				}
			}
		});
		this.#socket = new AdbDaemonSocket(this);
		this.#availableWriteBytes = options.availableWriteBytes;
	}
	async #writeChunk(data) {
		const length = data.length;
		while (this.#availableWriteBytes < length) {
			const resolver = new PromiseResolver();
			this.#availableWriteBytesChanged = resolver;
			await resolver.promise;
		}
		if (this.#availableWriteBytes === Infinity) this.#availableWriteBytes = -1;
		else this.#availableWriteBytes -= length;
		await this.#dispatcher.sendPacket(AdbCommand.Write, this.localId, this.remoteId, data);
	}
	async enqueue(data) {
		await this.#readableController.enqueue(data);
	}
	ack(bytes) {
		this.#availableWriteBytes += bytes;
		this.#availableWriteBytesChanged?.resolve();
	}
	async close() {
		if (this.#closed) return;
		this.#closed = true;
		this.#availableWriteBytesChanged?.reject(/* @__PURE__ */ new Error("Socket closed"));
		try {
			this.#writableController.error(/* @__PURE__ */ new Error("Socket closed"));
		} catch {}
		await this.#dispatcher.sendPacket(AdbCommand.Close, this.localId, this.remoteId, EmptyUint8Array);
	}
	dispose() {
		this.#readableController.close();
		this.#closedPromise.resolve(void 0);
	}
};
/**
* A duplex stream representing a socket to ADB daemon.
*/
var AdbDaemonSocket = class {
	#controller;
	get localId() {
		return this.#controller.localId;
	}
	get remoteId() {
		return this.#controller.remoteId;
	}
	get localCreated() {
		return this.#controller.localCreated;
	}
	get service() {
		return this.#controller.service;
	}
	get readable() {
		return this.#controller.readable;
	}
	get writable() {
		return this.#controller.writable;
	}
	get closed() {
		return this.#controller.closed;
	}
	constructor(controller) {
		this.#controller = controller;
	}
	close() {
		return this.#controller.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/dispatcher.js
/**
* The dispatcher is the "dumb" part of the connection handling logic.
*
* Except some options to change some minor behaviors,
* its only job is forwarding packets between authenticated underlying streams
* and abstracted socket objects.
*
* The `Adb` class is responsible for doing the authentication,
* negotiating the options, and has shortcuts to high-level services.
*/
var AdbPacketDispatcher = class {
	#initializers = new AsyncOperationManager(1);
	/**
	* Socket local ID to the socket controller.
	*/
	#sockets = /* @__PURE__ */ new Map();
	#writer;
	options;
	#closed = false;
	#disconnected = new PromiseResolver();
	get disconnected() {
		return this.#disconnected.promise;
	}
	#incomingSocketHandlers = /* @__PURE__ */ new Map();
	#readAbortController = new AbortController();
	constructor(connection, options) {
		this.options = options;
		if (this.options.initialDelayedAckBytes < 0) this.options.initialDelayedAckBytes = 0;
		connection.readable.pipeTo(new WritableStream({ write: async (packet, controller) => {
			switch (packet.command) {
				case AdbCommand.Close:
					await this.#handleClose(packet);
					break;
				case AdbCommand.Okay:
					this.#handleOkay(packet);
					break;
				case AdbCommand.Open:
					await this.#handleOpen(packet);
					break;
				case AdbCommand.Write:
					this.#handleWrite(packet).catch((e) => {
						controller.error(e);
					});
					break;
				default: throw new Error(`Unknown command: ${packet.command.toString(16)}`);
			}
		} }), {
			preventCancel: options.preserveConnection ?? false,
			signal: this.#readAbortController.signal
		}).then(() => {
			this.#dispose();
		}, (e) => {
			if (!this.#closed) this.#disconnected.reject(e);
			this.#dispose();
		});
		this.#writer = connection.writable.getWriter();
	}
	async #handleClose(packet) {
		if (packet.arg0 === 0 && this.#initializers.reject(packet.arg1, /* @__PURE__ */ new Error("Socket open failed"))) return;
		const socket = this.#sockets.get(packet.arg1);
		if (socket) {
			await socket.close();
			socket.dispose();
			this.#sockets.delete(packet.arg1);
			return;
		}
	}
	#handleOkay(packet) {
		let ackBytes;
		if (this.options.initialDelayedAckBytes !== 0) {
			if (packet.payload.length !== 4) throw new Error("Invalid OKAY packet. Payload size should be 4");
			ackBytes = /* @__PURE__ */ getUint32LittleEndian(packet.payload, 0);
		} else {
			if (packet.payload.length !== 0) throw new Error("Invalid OKAY packet. Payload size should be 0");
			ackBytes = Infinity;
		}
		if (this.#initializers.resolve(packet.arg1, {
			remoteId: packet.arg0,
			availableWriteBytes: ackBytes
		})) return;
		const socket = this.#sockets.get(packet.arg1);
		if (socket) {
			socket.ack(ackBytes);
			return;
		}
		this.sendPacket(AdbCommand.Close, packet.arg1, packet.arg0, EmptyUint8Array);
	}
	#sendOkay(localId, remoteId, ackBytes) {
		let payload;
		if (this.options.initialDelayedAckBytes !== 0) {
			payload = new Uint8Array(4);
			setUint32LittleEndian(payload, 0, ackBytes);
		} else payload = EmptyUint8Array;
		return this.sendPacket(AdbCommand.Okay, localId, remoteId, payload);
	}
	async #handleOpen(packet) {
		const [localId] = this.#initializers.add();
		this.#initializers.resolve(localId, void 0);
		const remoteId = packet.arg0;
		let availableWriteBytes = packet.arg1;
		let service = /* @__PURE__ */ decodeUtf8(packet.payload);
		if (service.endsWith("\0")) service = service.substring(0, service.length - 1);
		if (this.options.initialDelayedAckBytes === 0) {
			if (availableWriteBytes !== 0) throw new Error("Invalid OPEN packet. arg1 should be 0");
			availableWriteBytes = Infinity;
		} else if (availableWriteBytes === 0) throw new Error("Invalid OPEN packet. arg1 should be greater than 0");
		const handler = this.#incomingSocketHandlers.get(service);
		if (!handler) {
			await this.sendPacket(AdbCommand.Close, 0, remoteId, EmptyUint8Array);
			return;
		}
		const controller = new AdbDaemonSocketController({
			dispatcher: this,
			localId,
			remoteId,
			localCreated: false,
			service,
			availableWriteBytes
		});
		try {
			await handler(controller.socket);
			this.#sockets.set(localId, controller);
			await this.#sendOkay(localId, remoteId, this.options.initialDelayedAckBytes);
		} catch {
			await this.sendPacket(AdbCommand.Close, 0, remoteId, EmptyUint8Array);
		}
	}
	async #handleWrite(packet) {
		const socket = this.#sockets.get(packet.arg1);
		if (!socket) throw new Error(`Unknown local socket id: ${packet.arg1}`);
		let handled = false;
		const promises = [(async () => {
			await socket.enqueue(packet.payload);
			await this.#sendOkay(packet.arg1, packet.arg0, packet.payload.length);
			handled = true;
		})()];
		if (this.options.readTimeLimit) promises.push((async () => {
			await delay(this.options.readTimeLimit);
			if (!handled) throw new Error(`readable of \`${socket.service}\` has stalled for ${this.options.readTimeLimit} milliseconds`);
		})());
		await Promise.race(promises);
	}
	async createSocket(service) {
		if (this.options.appendNullToServiceString) service += "\0";
		const [localId, initializer] = this.#initializers.add();
		await this.sendPacket(AdbCommand.Open, localId, this.options.initialDelayedAckBytes, service);
		const { remoteId, availableWriteBytes } = await initializer;
		const controller = new AdbDaemonSocketController({
			dispatcher: this,
			localId,
			remoteId,
			localCreated: true,
			service,
			availableWriteBytes
		});
		this.#sockets.set(localId, controller);
		return controller.socket;
	}
	addReverseTunnel(service, handler) {
		this.#incomingSocketHandlers.set(service, handler);
	}
	removeReverseTunnel(address) {
		this.#incomingSocketHandlers.delete(address);
	}
	clearReverseTunnels() {
		this.#incomingSocketHandlers.clear();
	}
	async sendPacket(command, arg0, arg1, payload) {
		if (typeof payload === "string") payload = /* @__PURE__ */ encodeUtf8(payload);
		if (payload.length > this.options.maxPayloadSize) throw new TypeError("payload too large");
		await Consumable.WritableStream.write(this.#writer, {
			command,
			arg0,
			arg1,
			payload,
			checksum: this.options.calculateChecksum ? calculateChecksum(payload) : 0,
			magic: command ^ 4294967295
		});
	}
	async close() {
		await Promise.all(Array.from(this.#sockets.values(), (socket) => socket.close()));
		this.#closed = true;
		this.#readAbortController.abort();
		if (this.options.preserveConnection) this.#writer.releaseLock();
		else await this.#writer.close();
	}
	#dispose() {
		for (const socket of this.#sockets.values()) socket.dispose();
		this.#disconnected.resolve();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/daemon/transport.js
var ADB_DAEMON_VERSION_OMIT_CHECKSUM = 16777217;
var ADB_DAEMON_DEFAULT_FEATURES = /* @__PURE__ */ (() => [
	AdbFeature.ShellV2,
	AdbFeature.Cmd,
	AdbFeature.StatV2,
	AdbFeature.ListV2,
	AdbFeature.FixedPushMkdir,
	"apex",
	AdbFeature.Abb,
	"fixed_push_symlink_timestamp",
	AdbFeature.AbbExec,
	"remount_shell",
	"track_app",
	AdbFeature.SendReceiveV2,
	"sendrecv_v2_brotli",
	"sendrecv_v2_lz4",
	"sendrecv_v2_zstd",
	"sendrecv_v2_dry_run_send",
	AdbFeature.DelayedAck
])();
var ADB_DAEMON_DEFAULT_INITIAL_PAYLOAD_SIZE = 32 * 1024 * 1024;
/**
* An ADB Transport that connects to ADB Daemons directly.
*/
var AdbDaemonTransport = class AdbDaemonTransport {
	/**
	* Authenticate with the ADB Daemon and create a new transport.
	*/
	static async authenticate({ serial, connection, credentialStore, authenticators = ADB_DEFAULT_AUTHENTICATORS, features = ADB_DAEMON_DEFAULT_FEATURES, initialDelayedAckBytes = ADB_DAEMON_DEFAULT_INITIAL_PAYLOAD_SIZE, ...options }) {
		let version = 16777217;
		let maxPayloadSize = 1024 * 1024;
		const resolver = new PromiseResolver();
		const authProcessor = new AdbAuthenticationProcessor(authenticators, credentialStore);
		const abortController = new AbortController();
		const pipe = connection.readable.pipeTo(new WritableStream({ async write(packet) {
			switch (packet.command) {
				case AdbCommand.Connect:
					version = Math.min(version, packet.arg0);
					maxPayloadSize = Math.min(maxPayloadSize, packet.arg1);
					resolver.resolve(/* @__PURE__ */ decodeUtf8(packet.payload));
					break;
				case AdbCommand.Auth:
					await sendPacket(await authProcessor.process(packet));
					break;
				default: break;
			}
		} }), {
			preventCancel: true,
			signal: abortController.signal
		}).then(() => {
			resolver.reject(/* @__PURE__ */ new Error("Connection closed unexpectedly"));
		}, (e) => {
			resolver.reject(e);
		});
		const writer = connection.writable.getWriter();
		async function sendPacket(init) {
			init.checksum = calculateChecksum(init.payload);
			init.magic = init.command ^ 4294967295;
			await Consumable.WritableStream.write(writer, init);
		}
		const actualFeatures = features.slice();
		if (initialDelayedAckBytes <= 0) {
			const index = features.indexOf(AdbFeature.DelayedAck);
			if (index !== -1) actualFeatures.splice(index, 1);
		}
		let banner;
		try {
			await sendPacket({
				command: AdbCommand.Connect,
				arg0: version,
				arg1: maxPayloadSize,
				payload: /* @__PURE__ */ encodeUtf8(`host::features=${actualFeatures.join(",")}`)
			});
			banner = await resolver.promise;
		} finally {
			abortController.abort();
			writer.releaseLock();
			await pipe;
		}
		return new AdbDaemonTransport({
			serial,
			connection,
			version,
			maxPayloadSize,
			banner,
			features: actualFeatures,
			initialDelayedAckBytes,
			...options
		});
	}
	#connection;
	get connection() {
		return this.#connection;
	}
	#dispatcher;
	#serial;
	get serial() {
		return this.#serial;
	}
	#protocolVersion;
	get protocolVersion() {
		return this.#protocolVersion;
	}
	get maxPayloadSize() {
		return this.#dispatcher.options.maxPayloadSize;
	}
	#banner;
	get banner() {
		return this.#banner;
	}
	get disconnected() {
		return this.#dispatcher.disconnected;
	}
	#clientFeatures;
	get clientFeatures() {
		return this.#clientFeatures;
	}
	constructor({ serial, connection, version, banner, features = ADB_DAEMON_DEFAULT_FEATURES, initialDelayedAckBytes, ...options }) {
		this.#serial = serial;
		this.#connection = connection;
		this.#banner = AdbBanner.parse(banner);
		this.#clientFeatures = features;
		if (features.includes(AdbFeature.DelayedAck)) {
			if (initialDelayedAckBytes <= 0) throw new TypeError("`initialDelayedAckBytes` must be greater than 0 when DelayedAck feature is enabled.");
			if (!this.#banner.features.includes(AdbFeature.DelayedAck)) initialDelayedAckBytes = 0;
		} else initialDelayedAckBytes = 0;
		let calculateChecksum;
		let appendNullToServiceString;
		if (version >= 16777217) {
			calculateChecksum = false;
			appendNullToServiceString = false;
		} else {
			calculateChecksum = true;
			appendNullToServiceString = true;
		}
		this.#dispatcher = new AdbPacketDispatcher(connection, {
			calculateChecksum,
			appendNullToServiceString,
			initialDelayedAckBytes,
			...options
		});
		this.#protocolVersion = version;
	}
	connect(service) {
		return this.#dispatcher.createSocket(service);
	}
	addReverseTunnel(handler, address) {
		if (!address) address = `localabstract:reverse_${Math.random().toString().substring(2)}`;
		this.#dispatcher.addReverseTunnel(address, handler);
		return address;
	}
	removeReverseTunnel(address) {
		this.#dispatcher.removeReverseTunnel(address);
	}
	clearReverseTunnels() {
		this.#dispatcher.clearReverseTunnels();
	}
	close() {
		return this.#dispatcher.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/commands/m-dns.js
var MDnsCommands = class {
	#client;
	constructor(client) {
		this.#client = client;
	}
	async check() {
		const connection = await this.#client.createConnection("host:mdns:check");
		try {
			return !(await connection.readString()).startsWith("ERROR:");
		} finally {
			await connection.dispose();
		}
	}
	async getServices() {
		const connection = await this.#client.createConnection("host:mdns:services");
		try {
			return (await connection.readString()).split("\n").filter(Boolean).map((line) => {
				const parts = line.split("	");
				return {
					name: parts[0],
					service: parts[1],
					address: parts[2]
				};
			});
		} finally {
			await connection.dispose();
		}
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/stream.js
var OKAY = /* @__PURE__ */ encodeUtf8("OKAY");
var FAIL = /* @__PURE__ */ encodeUtf8("FAIL");
var AdbServerStream = class {
	#connection;
	#buffered;
	#writer;
	constructor(connection) {
		this.#connection = connection;
		this.#buffered = new BufferedReadableStream(connection.readable);
		this.#writer = connection.writable.getWriter();
	}
	readExactly(length) {
		return this.#buffered.readExactly(length);
	}
	readString = /* @__PURE__ */ bipedal(function* (then) {
		const length = hexToNumber(yield* then(this.readExactly(4)));
		if (length === 0) return "";
		else {
			const decoder = new TextDecoder();
			let result = "";
			const iterator = this.#buffered.iterateExactly(length);
			while (true) {
				const { done, value } = iterator.next();
				if (done) break;
				result += decoder.decode(yield* then(value), { stream: true });
			}
			result += decoder.decode();
			return result;
		}
	});
	async readOkay() {
		const response = await this.readExactly(4);
		if (sequenceEqual(response, OKAY)) return;
		if (sequenceEqual(response, FAIL)) {
			const reason = await this.readString();
			throw new Error(reason);
		}
		throw new Error(`Unexpected response: ${/* @__PURE__ */ decodeUtf8(response)}`);
	}
	async writeString(value) {
		const encoded = /* @__PURE__ */ encodeUtf8(value);
		const buffer = new Uint8Array(4 + encoded.length);
		write4HexDigits(buffer, 0, encoded.length);
		buffer.set(encoded, 4);
		await this.#writer.write(buffer);
	}
	release() {
		this.#writer.releaseLock();
		return {
			readable: this.#buffered.release(),
			writable: this.#connection.writable,
			closed: this.#connection.closed,
			close: () => this.#connection.close()
		};
	}
	async dispose() {
		tryCancel(this.#buffered);
		tryClose(this.#writer);
		await this.#connection.close();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/commands/wireless.js
var NetworkError = class extends Error {
	constructor(message) {
		super(message);
		this.name = "NetworkError";
	}
};
var UnauthorizedError = class extends Error {
	constructor(message) {
		super(message);
		this.name = "UnauthorizedError";
	}
};
var AlreadyConnectedError = class extends Error {
	constructor(message) {
		super(message);
		this.name = "AlreadyConnectedError";
	}
};
var WirelessCommands = class {
	#client;
	constructor(client) {
		this.#client = client;
	}
	/**
	* `adb pair <password> <address>`
	*/
	async pair(address, password) {
		const connection = await this.#client.createConnection(`host:pair:${password}:${address}`);
		try {
			const response = await connection.readExactly(4);
			if (sequenceEqual(response, FAIL)) throw new Error(await connection.readString());
			const length = hexToNumber(response);
			await connection.readExactly(length);
		} finally {
			await connection.dispose();
		}
	}
	/**
	* `adb connect <address>`
	*/
	async connect(address) {
		const connection = await this.#client.createConnection(`host:connect:${address}`);
		try {
			const response = await connection.readString();
			switch (response) {
				case `already connected to ${address}`: throw new AlreadyConnectedError(response);
				case `failed to connect to ${address}`:
				case `failed to authenticate to ${address}`: throw new UnauthorizedError(response);
				case `connected to ${address}`: return;
				default: throw new NetworkError(response);
			}
		} finally {
			await connection.dispose();
		}
	}
	/**
	* `adb disconnect <address>`
	*/
	async disconnect(address) {
		const connection = await this.#client.createConnection(`host:disconnect:${address}`);
		try {
			await connection.readString();
		} finally {
			await connection.dispose();
		}
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/observer.js
function unorderedRemove(array, index) {
	if (index < 0 || index >= array.length) return;
	array[index] = array[array.length - 1];
	array.length -= 1;
}
function filterDeviceStates(devices, states) {
	return devices.filter((device) => states.includes(device.state));
}
var AdbServerDeviceObserverOwner = class {
	current = [];
	#client;
	#stream;
	#observers = [];
	constructor(client) {
		this.#client = client;
	}
	async #receive(stream) {
		const response = await stream.readString();
		const next = AdbServerClient.parseDeviceList(response);
		const removed = this.current.slice();
		const added = [];
		for (const nextDevice of next) {
			const index = removed.findIndex((device) => device.transportId === nextDevice.transportId);
			if (index === -1) {
				added.push(nextDevice);
				continue;
			}
			unorderedRemove(removed, index);
		}
		this.current = next;
		if (added.length) for (const observer of this.#observers) {
			const filtered = filterDeviceStates(added, observer.includeStates);
			if (filtered.length) observer.onDeviceAdd.fire(filtered);
		}
		if (removed.length) {
			for (const observer of this.#observers) if (filterDeviceStates(removed, observer.includeStates).length) observer.onDeviceRemove.fire(removed);
		}
		for (const observer of this.#observers) {
			const filtered = filterDeviceStates(this.current, observer.includeStates);
			observer.onListChange.fire(filtered);
		}
	}
	async #receiveLoop(stream) {
		try {
			while (true) await this.#receive(stream);
		} catch (e) {
			this.#stream = void 0;
			for (const observer of this.#observers) observer.onError.fire(e);
		}
	}
	async #connect() {
		const stream = await this.#client.createConnection("host:track-devices-l", { unref: true });
		await this.#receive(stream);
		this.#receiveLoop(stream);
		return stream;
	}
	async #handleObserverStop(stream) {
		if (this.#observers.length === 0) {
			this.#stream = void 0;
			await stream.dispose();
		}
	}
	async createObserver(options) {
		options?.signal?.throwIfAborted();
		let current = [];
		const onDeviceAdd = new EventEmitter();
		const onDeviceRemove = new EventEmitter();
		const onListChange = new StickyEventEmitter();
		const onError = new StickyEventEmitter();
		const includeStates = options?.includeStates ?? ["device", "unauthorized"];
		const observer = {
			includeStates,
			onDeviceAdd,
			onDeviceRemove,
			onListChange,
			onError
		};
		this.#observers.push(observer);
		onListChange.event((value) => current = value);
		let stream;
		if (!this.#stream) {
			this.#stream = this.#connect();
			try {
				stream = await this.#stream;
			} catch (e) {
				this.#stream = void 0;
				throw e;
			}
		} else {
			stream = await this.#stream;
			onListChange.fire(filterDeviceStates(this.current, includeStates));
		}
		const ref = new Ref(options);
		const stop = async () => {
			unorderedRemove(this.#observers, this.#observers.indexOf(observer));
			await this.#handleObserverStop(stream);
			ref.unref();
		};
		if (options?.signal) {
			if (options.signal.aborted) {
				await stop();
				throw options.signal.reason;
			}
			options.signal.addEventListener("abort", () => void stop());
		}
		return {
			onDeviceAdd: onDeviceAdd.event,
			onDeviceRemove: onDeviceRemove.event,
			onListChange: onListChange.event,
			onError: onError.event,
			get current() {
				return current;
			},
			stop
		};
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/transport.js
var ADB_SERVER_DEFAULT_FEATURES = /* @__PURE__ */ (() => [
	AdbFeature.ShellV2,
	AdbFeature.Cmd,
	AdbFeature.StatV2,
	AdbFeature.ListV2,
	AdbFeature.FixedPushMkdir,
	"apex",
	AdbFeature.Abb,
	"fixed_push_symlink_timestamp",
	AdbFeature.AbbExec,
	"remount_shell",
	"track_app",
	AdbFeature.SendReceiveV2,
	"sendrecv_v2_brotli",
	"sendrecv_v2_lz4",
	"sendrecv_v2_zstd",
	"sendrecv_v2_dry_run_send"
])();
var AdbServerTransport = class {
	#client;
	serial;
	transportId;
	maxPayloadSize = 1 * 1024 * 1024;
	banner;
	#sockets = [];
	#closed = new PromiseResolver();
	#disconnected;
	get disconnected() {
		return this.#disconnected;
	}
	get clientFeatures() {
		return ADB_SERVER_DEFAULT_FEATURES;
	}
	constructor(client, serial, banner, transportId, disconnected) {
		this.#client = client;
		this.serial = serial;
		this.banner = banner;
		this.transportId = transportId;
		this.#disconnected = Promise.race([this.#closed.promise, disconnected]);
	}
	async connect(service) {
		const socket = await this.#client.createDeviceConnection({ transportId: this.transportId }, service);
		this.#sockets.push(socket);
		return socket;
	}
	async addReverseTunnel(handler, address) {
		return await this.#client.connector.addReverseTunnel(handler, address);
	}
	async removeReverseTunnel(address) {
		await this.#client.connector.removeReverseTunnel(address);
	}
	async clearReverseTunnels() {
		await this.#client.connector.clearReverseTunnels();
	}
	async close() {
		for (const socket of this.#sockets) await socket.close();
		this.#sockets.length = 0;
		this.#closed.resolve();
	}
};
//#endregion
//#region node_modules/@yume-chan/adb/esm/server/client.js
/**
* Client for the ADB Server.
*/
var AdbServerClient = class AdbServerClient {
	static NetworkError = NetworkError;
	static UnauthorizedError = UnauthorizedError;
	static AlreadyConnectedError = AlreadyConnectedError;
	static parseDeviceList(value, includeStates = ["device", "unauthorized"]) {
		const devices = [];
		for (const line of value.split("\n")) {
			if (!line) continue;
			const parts = line.split(" ").filter(Boolean);
			const serial = parts[0];
			const state = parts[1];
			if (!includeStates.includes(state)) continue;
			let product;
			let model;
			let device;
			let transportId;
			for (let i = 2; i < parts.length; i += 1) {
				const [key, value] = parts[i].split(":");
				switch (key) {
					case "product":
						product = value;
						break;
					case "model":
						model = value;
						break;
					case "device":
						device = value;
						break;
					case "transport_id":
						transportId = BigInt(value);
						break;
				}
			}
			if (!transportId) throw new Error(`No transport id for device ${serial}`);
			devices.push({
				serial,
				state,
				authenticating: state === "unauthorized",
				product,
				model,
				device,
				transportId
			});
		}
		return devices;
	}
	static formatDeviceService(device, command) {
		if (!device) return `host:${command}`;
		if ("transportId" in device) return `host-transport-id:${device.transportId}:${command}`;
		if ("serial" in device) return `host-serial:${device.serial}:${command}`;
		if ("usb" in device) return `host-usb:${command}`;
		if ("tcp" in device) return `host-local:${command}`;
		throw new TypeError("Invalid device selector");
	}
	connector;
	wireless = new WirelessCommands(this);
	mDns = new MDnsCommands(this);
	#observerOwner = new AdbServerDeviceObserverOwner(this);
	constructor(connector) {
		this.connector = connector;
	}
	async createConnection(request, options) {
		const stream = new AdbServerStream(await this.connector.connect(options));
		try {
			await stream.writeString(request);
		} catch (e) {
			await stream.dispose();
			throw e;
		}
		try {
			await raceSignal(() => stream.readOkay(), options?.signal);
			return stream;
		} catch (e) {
			await stream.dispose();
			throw e;
		}
	}
	/**
	* `adb version`
	*/
	async getVersion() {
		const connection = await this.createConnection("host:version");
		try {
			const length = hexToNumber(await connection.readExactly(4));
			return hexToNumber(await connection.readExactly(length));
		} finally {
			await connection.dispose();
		}
	}
	async validateVersion(minimalVersion) {
		const version = await this.getVersion();
		if (version < minimalVersion) throw new Error(`adb server version (${version}) doesn't match this client (${minimalVersion})`);
	}
	/**
	* `adb kill-server`
	*/
	async killServer() {
		await (await this.createConnection("host:kill")).dispose();
	}
	/**
	* `adb host-features`
	*/
	async getServerFeatures() {
		const connection = await this.createConnection("host:host-features");
		try {
			return (await connection.readString()).split(",");
		} finally {
			await connection.dispose();
		}
	}
	/**
	* Get a list of connected devices from ADB Server.
	*
	* Equivalent ADB Command: `adb devices -l`
	*/
	async getDevices(includeStates = ["device", "unauthorized"]) {
		const connection = await this.createConnection("host:devices-l");
		try {
			const response = await connection.readString();
			return AdbServerClient.parseDeviceList(response, includeStates);
		} finally {
			await connection.dispose();
		}
	}
	/**
	* Monitors device list changes.
	*/
	async trackDevices(options) {
		return this.#observerOwner.createObserver(options);
	}
	/**
	* `adb -s <device> reconnect` or `adb reconnect offline`
	*/
	async reconnectDevice(device) {
		const connection = await this.createConnection(device === "offline" ? "host:reconnect-offline" : AdbServerClient.formatDeviceService(device, "reconnect"));
		try {
			await connection.readString();
		} finally {
			await connection.dispose();
		}
	}
	/**
	* Gets the features supported by the device.
	* The transport ID of the selected device is also returned,
	* so the caller can execute other commands against the same device.
	* @param device The device selector
	* @returns The transport ID of the selected device, and the features supported by the device.
	*/
	async getDeviceFeatures(device) {
		const connection = await this.createDeviceConnection(device, "host:features");
		const stream = new AdbServerStream(connection);
		try {
			const features = (await stream.readString()).split(",");
			return {
				transportId: connection.transportId,
				features
			};
		} finally {
			await stream.dispose();
		}
	}
	/**
	* Creates a connection that will forward the service to device.
	* @param device The device selector
	* @param service The service to forward
	* @returns An `AdbServerClient.Socket` that can be used to communicate with the service
	*/
	async createDeviceConnection(device, service) {
		let switchService;
		let transportId;
		if (!device) {
			await this.validateVersion(41);
			switchService = `host:tport:any`;
		} else if ("transportId" in device) {
			switchService = `host:transport-id:${device.transportId}`;
			transportId = device.transportId;
		} else if ("serial" in device) {
			await this.validateVersion(41);
			switchService = `host:tport:serial:${device.serial}`;
		} else if ("usb" in device) {
			await this.validateVersion(41);
			switchService = `host:tport:usb`;
		} else if ("tcp" in device) {
			await this.validateVersion(41);
			switchService = `host:tport:local`;
		} else throw new TypeError("Invalid device selector");
		const connection = await this.createConnection(switchService);
		try {
			await connection.writeString(service);
		} catch (e) {
			await connection.dispose();
			throw e;
		}
		try {
			if (transportId === void 0) transportId = getUint64LittleEndian(await connection.readExactly(8), 0);
			await connection.readOkay();
			const socket = connection.release();
			return {
				transportId,
				service,
				readable: socket.readable,
				writable: socket.writable,
				get closed() {
					return socket.closed;
				},
				async close() {
					await socket.close();
				}
			};
		} catch (e) {
			await connection.dispose();
			throw e;
		}
	}
	async #waitForUnchecked(device, state, options) {
		let type;
		if (!device) type = "any";
		else if ("transportId" in device) type = "any";
		else if ("serial" in device) type = "any";
		else if ("usb" in device) type = "usb";
		else if ("tcp" in device) type = "local";
		else throw new TypeError("Invalid device selector");
		const service = AdbServerClient.formatDeviceService(device, `wait-for-${type}-${state}`);
		const connection = await this.createConnection(service, options);
		try {
			await connection.readOkay();
		} finally {
			await connection.dispose();
		}
	}
	/**
	* Wait for a device to be connected or disconnected.
	*
	* `adb wait-for-<state>`
	*
	* @param device The device selector
	* @param state The state to wait for
	* @param options The options
	* @returns A promise that resolves when the condition is met.
	*/
	async waitFor(device, state, options) {
		if (state === "disconnect") await this.validateVersion(41);
		return this.#waitForUnchecked(device, state, options);
	}
	async waitForDisconnect(transportId, options) {
		if (await this.getVersion() >= 41) return this.#waitForUnchecked({ transportId }, "disconnect", options);
		else {
			const observer = await this.trackDevices(options);
			return new Promise((resolve, reject) => {
				observer.onDeviceRemove((devices) => {
					if (devices.some((device) => device.transportId === transportId)) {
						observer.stop();
						resolve();
					}
				});
				observer.onError((e) => {
					observer.stop();
					reject(e);
				});
			});
		}
	}
	/**
	* Creates an ADB Transport for the specified device.
	*/
	async createTransport(device) {
		const { transportId, features } = await this.getDeviceFeatures(device);
		const info = (await this.getDevices()).find((device) => device.transportId === transportId);
		const banner = new AdbBanner(info?.state, info?.product, info?.model, info?.device, features);
		const waitAbortController = new AbortController();
		const disconnected = this.waitForDisconnect(transportId, {
			unref: true,
			signal: waitAbortController.signal
		});
		const transport = new AdbServerTransport(this, info?.serial ?? "", banner, transportId, disconnected);
		transport.disconnected.finally(() => waitAbortController.abort());
		return transport;
	}
	async createAdb(device) {
		return new Adb(await this.createTransport(device));
	}
};
async function raceSignal(callback, ...signals) {
	const abortPromise = new PromiseResolver();
	function abort() {
		abortPromise.reject(this.reason);
	}
	try {
		for (const signal of signals) {
			if (!signal) continue;
			if (signal.aborted) throw signal.reason;
			signal.addEventListener("abort", abort);
		}
		return await Promise.race([callback(), abortPromise.promise]);
	} finally {
		for (const signal of signals) {
			if (!signal) continue;
			signal.removeEventListener("abort", abort);
		}
	}
}
//#endregion
export { adbSyncPushV1 as $, AdbPower as $t, SHA1_DIGEST_INFO as A, AdbShellProtocolPacket as At, AdbBanner as B, AdbReverseNotSupportedError as Bt, AdbPacketHeader as C, adbSyncReadResponses as Ct, ASN1_OCTET_STRING as D, AdbShellProtocolPtyProcess as Dt, ASN1_NULL as E, AdbShellProtocolSpawner as Et, modInverse as F, splitCommand as Ft, dirname as G, unreachable as Gt, Adb as H, sequenceEqual as Ht, powMod as I, AdbNoneProtocolPtyProcess as It, ADB_SYNC_MAX_PACKET_SIZE as J, calculateBase64EncodedLength as Jt, AdbSyncSocket as K, hexToNumber as Kt, rsaParsePrivateKey as L, AdbNoneProtocolProcessImpl as Lt, adbGeneratePublicKey as M, AdbNoneProtocolSubprocessService as Mt, adbGetPublicKeySize as N, AdbNoneProtocolSpawner as Nt, ASN1_OID as O, AdbShellProtocolProcessImpl as Ot, getBigUint as P, escapeArg as Pt, adbSyncPush as Q, toLocalUint8Array as Qt, rsaSign as R, AdbReverseError as Rt, AdbPacket as S, adbSyncReadResponse as St, calculateChecksum as T, AdbShellProtocolSubprocessService as Tt, AdbTcpIpService as U, Ref as Ut, AdbBannerKey as V, AdbReverseService as Vt, AdbSync as W, NOOP as Wt, AdbSyncSendV2Flags as X, encodeBase64 as Xt, AdbSyncOkResponse as Y, decodeBase64 as Yt, AdbSyncSendV2Request as Z, AutoResetEvent as Zt, AdbAuthType as _, PromiseResolver as _n, adbSyncWriteRequest as _t, AdbServerDeviceObserverOwner as a, framebuffer as an, AdbSyncEntryResponse as at, AdbSignatureAuthenticator as b, AdbSyncResponseId as bt, FAIL as c, EventEmitter as cn, adbSyncOpenDirV2 as ct, ADB_DAEMON_VERSION_OMIT_CHECKSUM as d, ReadableStream as dn, AdbSyncStatResponse as dt, AdbFrameBufferError as en, adbSyncPushV2 as et, AdbDaemonTransport as f, WritableStream as fn, LinuxFileType as ft, ADB_DEFAULT_AUTHENTICATORS as g, EmptyUint8Array as gn, AdbSyncRequestId as gt, AdbDaemonSocketController as h, Uint8ArrayExactReadable as hn, AdbSyncNumberRequest as ht, AdbServerTransport as i, AdbFrameBufferV2 as in, AdbSyncEntry2Response as it, SHA1_DIGEST_LENGTH as j, AdbFeature as jt, ASN1_SEQUENCE as k, AdbShellProtocolId as kt, ADB_DAEMON_DEFAULT_FEATURES as l, MaybeConsumableWritableStream as ln, AdbSyncLstatResponse as lt, AdbDaemonSocket as m, encodeUtf8 as mn, adbSyncStat as mt, raceSignal as n, AdbFrameBufferUnsupportedVersionError as nn, adbSyncPull as nt, unorderedRemove as o, AdbServiceBase as on, adbSyncOpenDir as ot, AdbPacketDispatcher as p, decodeUtf8 as pn, adbSyncLstat as pt, AdbSyncSocketLocked as q, write4HexDigits as qt, ADB_SERVER_DEFAULT_FEATURES as r, AdbFrameBufferV1 as rn, adbSyncPullGenerator as rt, AdbServerStream as s, StickyEventEmitter as sn, adbSyncOpenDirV1 as st, AdbServerClient as t, AdbFrameBufferForbiddenError as tn, AdbSyncDataResponse as tt, ADB_DAEMON_DEFAULT_INITIAL_PAYLOAD_SIZE as u, tryClose as un, AdbSyncStatErrorCode as ut, AdbAuthenticationProcessor as v, AdbSyncError as vt, AdbPacketSerializeStream as w, AdbSubprocessService as wt, AdbCommand as x, adbSyncEncodeId as xt, AdbPublicKeyAuthenticator as y, AdbSyncFailResponse as yt, setBigUint as z, AdbReverseErrorResponse as zt };

//# sourceMappingURL=esm-C_prFY3i.js.map